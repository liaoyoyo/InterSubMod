# SURVEY DIGEST


## CAT: External tool deep-survey: ONT modkit (modified-base toolkit) — #1 comparison target for ISM (InterSubMod)
- **modkit (overall)** [ONT long-read modified-base CLI toolkit (Rust)] gran=Primarily POSITION-level (pileup aggregates per genomic posi… ✓
    method: Converts modBAM (MM/ML tags from ONT basecallers) to bedMethyl pileup via best-practice probability thresholding, plus manipulation/QC/stats subcommands. NOT a clustering/structure engine — a per-position aggregation + Bayesian-comparison toolkit.
    stat: Bayesian conjugate-prior likelihood ratio (DMR), MAP-based effect-size p-value, Cohen's h, 2-state HMM segmentation, Shannon methylation entropy.
    hap: PARTIAL / upstream-only. pileup --phased reads HP tag -> hp1.bedmethyl / hp2.bedmethyl / combined.bedmethyl. NO --haplotype flag inside dmr/…
    vs_ISM: modkit aggregates calls PER POSITION then compares groups; ISM keeps read x CpG matrix and measures BETWEEN-MOLECULE structure (read-read distance + clustering + PERMANOVA). modkit asks 'is mean methylation different between two groups/haplotypes here'; ISM asks 'are reads structurally organized int…
    cite: https://nanoporetech.github.io/modkit/ ; https://github.com/nanoporetech/modkit
- **modkit pileup** [Position-level aggregation] gran=Position-level (collapses reads). ✓
    method: Aggregates per-read MM/ML calls to per-position counts (Nmod, Ncanonical, Nother, Nfiltered) after a pass-probability threshold (auto percentile or explicit). Produces bedMethyl.
    stat: Counts + percent-modified per position; threshold via sample-probs percentile (default ~10th).
    hap: YES via --phased: partitions output by HP tag into hp1/hp2/combined. The ONLY place HP is consumed.
    vs_ISM: ISM also reads MM/ML but BINARIZES (>0.8 meth / <0.2 unmeth) and keeps the full read x CpG matrix instead of collapsing to per-position counts — preserving molecule-level co-occurrence that pileup discards.
    cite: https://github.com/nanoporetech/modkit
- **modkit pileup-hemi** [Read-level (single-molecule, double-stranded) hemi…] gran=Read-level / single-molecule (both strands of one molecule),… ✓
    method: At palindromic motifs (default CpG, --cpg = --motif CG 0) counts double-stranded methylation patterns on the SAME molecule: requires both + and - strand calls above pass threshold (duplex). Reports 4 patterns m,m / m,- / -,m / -,-.
    stat: Pattern-specific counts (Npattern, Ncanonical, Nother_pattern...) in ~18-col bedMethyl.
    hap: No HP interaction documented.
    vs_ISM: Both molecule-level, but pileup-hemi measures strand-pair asymmetry (top vs bottom of ONE CpG); ISM measures read-to-read organization ACROSS many CpGs (subclonal/haplotype structure). Orthogonal axes.
    cite: https://nanoporetech.github.io/modkit/intro_pileup_hemi.html
- **modkit dmr (pair / multi)** [Differential methylation scoring (Bayesian likelih…] gran=POSITION-level (single-site, no --regions) OR REGION-level (… ✓
    method: BAYESIAN conjugate-prior LIKELIHOOD RATIO on per-position counts (NOT a frequentist Fisher/chi-square LRT). Binary: X|p~Bin(n,p), p~Beta(a,b); multi-state (h/m/C): X|pi~Mult(n,pi), pi~Dir(alpha); Jeffreys prior. score = log[ l_a(Xa)*l_b(Xb) / l_{a+b}(X_{a+b}) ] of log marginal likelihoods (higher=more differential). Si…
    stat: Bayesian marginal-likelihood ratio score; MAP-based p-value (single-site); Cohen's h + 95% CI (region); 2-state HMM segmentation. dmr pair = 2 samples (single-site OR region); dmr …
    hap: NO internal --haplotype flag (verified across intro_dmr, advanced_usage, GitHub book src). Haplotype DMR = upstream pileup --phased -> hp1/h…
    vs_ISM: CRITICAL DIFF. (1) dmr compares per-position COUNT proportions between two pre-defined groups via Bayesian LR + MAP effect-size; ISM Delta-beta does paired per-CpG MEAN difference + Wilcoxon signed-rank + Cohen d, AND tests STRUCTURE (PERMANOVA on read-read distance) which dmr cannot. (2) Haplotype:…
    cite: https://nanoporetech.github.io/modkit/dmr_scoring_details.html ; https://nanoporetech.github.io/modkit/intro_dmr.html
- **modkit entropy** [Read-level methylation entropy (Shannon)] gran=Read-level co-occurrence (epialleles on single molecules), a… ✓
    method: YES — modkit HAS a methylation-entropy module. Shannon entropy of epiallele patterns across reads in a window: ME = -(1/N) sum_i Pr(n_i) log2 Pr(n_i), sum over distinct methylation patterns n_i on reads spanning the window, Pr(n_i)=empirical pattern frequency. Reads co-occurrence on INDIVIDUAL reads (input is modBAM, N…
    stat: Shannon entropy (bits). Window output=bedGraph (chrom,start,end,entropy,num_reads); region output=mean/median/min/max entropy + read/window counts. NME/MSE normalized variants NOT …
    hap: No haplotype option documented for entropy.
    vs_ISM: OVERLAP but ISM broader. modkit entropy = pattern-frequency Shannon entropy on n-position windows ('disorder'). ISM ALSO computes NME + epipolymorphism on 4-CpG windows ('disorder' family) but only as CONTRAST against its primary 'structure' engine (read-read distance + clustering + PERMANOVA), whic…
    cite: https://nanoporetech.github.io/modkit/intro_entropy.html
- **modkit sample-probs** [QC / probability distribution estimation] gran=Per-call probability distribution (no genomic structure). ✓
    method: Samples modification-probability values from a modBAM, computes empirical percentiles/quantiles (default percentiles 10,50,90) + optional histogram. Used to choose the pileup pass threshold.
    stat: Empirical percentiles/quantiles; histogram.
    hap: No.
    vs_ISM: ISM uses fixed binarization cutoffs (>0.8/<0.2) rather than sample-derived percentiles; conceptually the threshold-calibration step, no direct analog.
    cite: https://nanoporetech.github.io/modkit/advanced_usage.html
- **modkit adjust-mods** [modBAM transformation] gran=Per-call tag editing. ✓
    method: Edits MM/ML tags: --ignore <code> removes a mod; --convert <from> <to> remaps codes (summing probabilities); --motif/--cpg keeps only motif-context calls; --filter-probs drops lowest-confidence calls.
    stat: None (deterministic transform).
    hap: No.
    vs_ISM: ISM reads raw MM/ML directly (keeps 5mC AND 5hmC separately); no equivalent rewrite. ISM's known 5mC+5hmC collapse artifact maps to this subcommand's --convert behavior.
    cite: https://nanoporetech.github.io/modkit/advanced_usage.html
- **modkit repair** [modBAM tag transfer] gran=Per-read tag projection. ✓
    method: Transfers MM/ML tags from a donor modBAM to an acceptor modBAM by matching reads on sequence identity (both sorted by name; donor must be superset; ambiguous matches rejected+logged). For when re-alignment/trimming stripped mod tags.
    stat: None.
    hap: No.
    vs_ISM: No analog; pure data-plumbing utility.
    cite: https://nanoporetech.github.io/modkit/advanced_usage.html
- **modkit validate** [Accuracy benchmarking against ground truth] gran=Per-call classification vs truth. ✓
    method: Compares modBAM predictions to a ground-truth BED (known modified/canonical sites), applies a filter threshold (default filter-quantile 0.1 or explicit --filter-threshold), reports detection accuracy. -c/--canonical-base for canonical-only truth; --edge-filter excludes read ends.
    stat: Accuracy / confusion metrics at chosen probability threshold.
    hap: No.
    vs_ISM: ISM has no basecaller-validation step; trusts MM/ML. Different scope.
    cite: https://nanoporetech.github.io/modkit/advanced_usage.html
- **modkit motif (bed / search / refine)** [Motif analysis (Bayesian motif model)] gran=Sequence-context (k-mer) level, aggregated over genome. ✓
    method: motif bed = list all genomic locations of a sequence motif. motif search = scan bedMethyl+ref for modification-ENRICHED sequence contexts via a Bayesian motif model with --low-thresh (0.2) / --high-thresh (0.6) and --context-size (12,12 default). motif refine = optimize/trim motif definitions by enrichment.
    stat: Bayesian enrichment scoring of contexts vs thresholds.
    hap: No.
    vs_ISM: No analog; ISM does not do sequence-motif discovery. Out of scope for somatic-CpG comparison.
    cite: https://nanoporetech.github.io/modkit/advanced_usage.html

## CAT: Long-read modBAM methylation extraction / DMR / visualization toolkits (non-modkit) — surveyed for ONT cancer-somatic co…
- **modbam2bed** [Extraction / aggregation tool (modBAM -> bedMethyl…] gran=Site/region-level aggregate ONLY (per reference position acr… ✓
    method: htslib pileup API over MM/ML tags. Per-strand base-count matrix; each base classed canonical / modified / filtered(ambiguous) / no-call. Modification frequency = modified / (canonical + modified); low-confidence calls below the probability threshold are dropped (filtered) but substitutions+deletions count toward covera…
    stat: None inferential. Deterministic count-based methylation %. -f/--threshold default 0.66 (below = filtered/ambiguous); older -a canon_threshold / -b mod_threshold deprecated for unif…
    hap: YES, by pre-filtering (one hap per run, not one-pass split). Flags: --haplotype=VAL (process reads of one HP value); generic --tag_name=TN /…
    vs_ISM: Complementary, not competing. modbam2bed = the EXTRACTION/AGGREGATION primitive that ISM's MethylationMatrix replaces at READ level. modbam2bed collapses reads to a per-site % (loses between-molecule structure); ISM keeps the full read x CpG matrix + read-to-READ distance + PERMANOVA. No clustering,…
    cite: GitHub epi2me-labs/modbam2bed README (raw master). No peer-reviewed paper; ONT EPI2ME-Labs tool.
- **modbamtools** [Read-level visualization + lightweight per-region …] gran=Read-level for plot and cluster (single-molecule); region-le… ✓
    method: Parses MM/ML (pysam/htslib) into per-read modification state. Subcommands: plot (single-read base-modification browser-style figures), calcMeth (per-region aggregate methylation %), cluster (groups reads by modification state).
    stat: cluster = HDBSCAN (hierarchical density-based spatial clustering; scikit-learn-contrib/hdbscan) on reads 'based on modification state' -> outputs n_clusters + n_reads passing QC ap…
    hap: YES via --hap (separate reads by HP tag). plot supports --hap + --strands; calcMeth --hap emits per-haplotype stats (9 cols = 3/hap vs 3). c…
    vs_ISM: Closest conceptual neighbour to ISM's clustering core but weaker for ISM's question. modbamtools cluster = HDBSCAN returning a cluster COUNT; ISM = explicit read-to-read DISTANCE matrix (NHD/L1/Bernoulli/Pearson/Jaccard, min 5 common CpG) -> UPGMA/Ward -> silhouette k=2..10 -> PERMANOVA (pseudo-F, 9…
    cite: Razaghi R, Hook PW, Ou S, Schatz MC, Hansen KD, Jain M, Timp W. Modbamtools. bioRxiv 2022.07.07.499188 (2022). DOI 10.1101/2022.07.07.499188. PREPRINT (not jour…
- **methylartist** [Visualization suite (read-level + aggregate); prod…] gran=Both: read-level single-molecule locus plots AND aggregate (… ✓
    method: Python suite consuming modBAM MM/ML or tabular per-read calls (megalodon/nanopolish/guppy: read name, base position, mod probability). Locus plots (single aligned reads), segment-based + composite repeat-element aggregate plots, plus bedMethyl and DSS-input files.
    stat: NO native DMR test. Emits files 'suitable for input to DSS' (Park & Wu) for differential methylation; statistics delegated to DSS externally. Visualization-primary.
    hap: YES. Locus plots separate methylation by phase if BAMs are first phased (PS + HP tags via WhatsHap or other). Splitting is by pre-added HP/P…
    vs_ISM: Pure visualization overlap; near-zero analytic overlap. Best off-the-shelf way to render ISM's per-region read x CpG data as a phased single-molecule locus plot (which ISM itself does not do). NO distance matrix, NO PERMANOVA, NO clustering with significance, NO somatic/normal-baseline residual; onl…
    cite: Cheetham SW, Kindlova M, Ewing AD. Methylartist: tools for visualizing modified bases from nanopore sequence data. Bioinformatics 2022;38(11):3109-3112. DOI 10.…
- **NanoMethViz** [R/Bioconductor visualization package; format conve…] gran=Both: read-level spaghetti plots AND aggregate/feature-level… ✓
    method: Converts caller output (nanopolish, f5c, megalodon) into a compact intermediate (sample, 1-based chrom position, methylation log-likelihood-ratio, read name; bgzip+tabix). Produces spaghetti (per-read) plots with smoothed methylation probability + aggregate feature-level plots with binned means.
    stat: NONE native. 'Not a complete pipeline; modular component.' Delegates DMR to bsseq, DSS and edgeR (DSS = beta-binomial smoothing + Wald test; edgeR = NB GLM; paper does not detail).…
    hap: PARTIAL / UNVERIFIED as native. Paper demonstrates haplotyped data (maternal vs paternal X via WhatsHap) and renders haplotype-separated res…
    vs_ISM: Visualization-and-handoff overlap only. R-side counterpart to methylartist: good for spaghetti/aggregate figures and piping to bsseq/DSS/edgeR (site/region group-level DMR, count-based NB/beta-binomial, germline-oriented). NO read-to-read distance, NO PERMANOVA structure test, NO somatic normal-base…
    cite: Su S, Gouil Q, Blewitt ME, Cook D, Hickey PF, Ritchie ME. NanoMethViz: An R/Bioconductor package for visualizing long-read methylation data. PLOS Comput Biol 20…
- **pb-CpG-tools (aligned_bam_to_cpg_scores)** [Extraction / per-site scoring tool. Site-level. NO…] gran=Site-level (per CpG). No per-read output. ✓
    method: Reads C+m (5mC) from MM/ML tags of aligned HiFi BAM/CRAM. Two pileup modes: 'model' (default/recommended) uses distributions of modification scores + a built-in ML model to compute per-CpG-site modification probability; 'count' classifies a base as modified if mod-prob > 0.5 and reports site methylation = fraction modi…
    stat: 'model' mode = built-in ML model over score distributions giving per-site probability (architecture/training NOT documented = internals UNVERIFIED). 'count' mode = deterministic 0.…
    hap: YES, automatic split. When HP tags present (default tag HP, configurable via --hap-tag), outputs separate [prefix].hap1.* and [prefix].hap2.…
    vs_ISM: Cross-PLATFORM (PacBio HiFi, NOT ONT) and 5mC-ONLY -> only weakly comparable to ISM. An extraction/scoring analogue of modbam2bed for PacBio: site-level % + automatic hap1/hap2 tracks. NO 5hmC (ISM uses 5mC AND 5hmC), no read-level matrix, no distance/PERMANOVA/clustering, no somatic normal-baseline…
    cite: GitHub PacificBiosciences/pb-CpG-tools README (main). No standalone peer-reviewed paper for the tool itself.
- **ONT EPI2ME wf-human-variation (methylation module = modkit)** [Nextflow workflow wrapper; methylation step is mod…] gran=Site/region-level aggregate bedMethyl. modkit dmr discards w… ✓
    method: Workflow calls modkit for modified-base aggregation. Default: modkit pileup --cpg --combine-strands (strand-collapsed CpG bedMethyl). --force_strand reverts to per-strand. --phased partitions phased reads into hp1.bedmethyl / hp2.bedmethyl / combined.bedmethyl (= modkit --partition-tag HP). DMR is a separate downstream…
    stat: modkit dmr pair: log MARGINAL LIKELIHOOD RATIO comparing two conditions modeled separately vs jointly = log(L_a * L_b / L_{a+b}); conjugate priors with Jeffrey's prior — Beta-Binom…
    hap: YES. --phased -> hp1/hp2/combined bedMethyl (modkit --partition-tag HP under the hood; reads phased by whatshap/longphase within workflow). …
    vs_ISM: The production reference pipeline ISM sits beside; contrast is sharpest here. wf-human-variation/modkit = extraction + haplotype-split bedMethyl + region DMR via a Dirichlet-Multinomial/Beta-Binomial marginal-likelihood-ratio that EXPLICITLY DISCARDS within-region read ordering — i.e. aggregate per-…
    cite: EPI2ME wf-human-variation docs (epi2me.nanoporetech.com); modkit DMR scoring docs (nanoporetech.github.io/modkit/dmr_scoring_details.html); modkit pileup --part…

## CAT: long-read READ-LEVEL methylation clustering & disorder tools (vs ISM)
- **pycoMeth (Meth_Seg + Meth_Comp + Comp_Report)** [long-read ONT (Nanopore), germline; region-level d…] gran=Region/interval-level OUTPUT (DMRs/DMIs as BED/TSV). Read-le… ✓
    method: Meth_Seg = Bayesian changepoint / methylome segmentation modeled as an HMM solved by Baum-Welch (EM) with forward-backward posteriors; emission models methylation-call uncertainty via Nanopolish LLRs (sigmoid->prob); parameterizes methylation rate mu(s,g) per segment s x read-group g to produce ONE consensus segmentati…
    stat: Meth_Comp three modes: (a) llr_diff = ranked test on mean per-read LLR differences; (b) bs_diff = two-sided Mann-Whitney-U (2 samples) / Kruskal-Wallis (n>2) on binarized methylati…
    hap: YES - explicit. Haplotype assignment per read stored as MetH5 read-groups (from WhatsHap haplotagging of input BAM); segmentation produces c…
    vs_ISM: Closest on HAPLOTYPE-AWARENESS (genuine: MetH5 read-groups = WhatsHap haplotags -> consensus segmentation treats each haplotype as a read-group). BUT: pycoMeth does NOT build a read-to-read distance matrix and does NOT cluster reads (ISM CORE 1-3 absent). Its DM test is rank/contingency per-interval…
    cite: Snajder R, et al. pycoMeth: a toolbox for differential methylation testing from Nanopore methylation calls. Genome Biology 2023;24:83. DOI 10.1186/s13059-023-02…
- **cvlr (cluster variant long reads)** [long-read ONT (Nanopore), germline; read-level mod…] gran=Dual: READ-LEVEL cluster assignment (posterior membership pe… ✓
    method: Model-based clustering: fits a MIXTURE OF MULTIVARIATE BERNOULLI distributions over reads via an Expectation-Maximization (EM) algorithm that tolerates incomplete (missing) CpG observations. Input is a binary read x position matrix X (Xij in {0 unmeth, 1 meth, -1 unknown}); user sets k (e.g. k=2). EM yields posterior c…
    stat: EM for Bernoulli mixture (posterior responsibilities). Heterogeneity statistic = median absolute methylation difference between clusters across positions; threshold at 95th percent…
    hap: NO (by design). Clustering does NOT use phase/HP and explicitly 'does not rely on a previous phasing step.' Haplotype used only POST-HOC for…
    vs_ISM: The ONLY one of the three that does per-READ clustering into subpopulations -> conceptual cousin of ISM CORE 2 (subclonal read groups). BUT cvlr is MODEL-BASED (EM Bernoulli mixture, posterior membership) and explicitly does NOT compute a read-to-read pairwise distance matrix -> fundamentally differ…
    cite: Raineri E, Alberola i Pla M, Dabad M, Heath S. cvlr: finding heterogeneously methylated genomic regions using ONT reads. Bioinformatics Advances 2023;3(1):vbac1…
- **Metheor** [SHORT-READ bisulfite (Bismark alignments), cancer …] gran=Multi-level OUTPUT: CpG-wise (PDR, MHL, FDRP, qFDRP), CpG-qu… ✓
    method: Single-pass ('one-sweep') read-centric scan over Bismark-tagged bisulfite alignments computing intra-molecule (within-read) co-methylation disorder. Implemented in Rust. Computes a family of heterogeneity scores; NO model fitting, NO clustering, NO distance matrix. LPMD (novel) = proportion of reads showing DIFFERENT m…
    stat: Descriptive heterogeneity metrics (no inferential test in core): PDR (proportion discordant reads), ME (Shannon methylation entropy over CpG window), PM/epipolymorphism, FDRP, qFDR…
    hap: NO genuine phasing. Only MHL (Methylation Haplotype Load) measures conserved consecutive-CpG methylated stretches within a read = co-methyla…
    vs_ISM: Overlaps ISM CORE 4 'disorder family' (ME = normalized methylation entropy ~ ISM NME; PM = epipolymorphism over a 4-CpG window = ISM 4-CpG epipolymorphism; LPMD/PDR = within-read discordance). BUT Metheor is BISULFITE SHORT-READ (not ONT long-read, no 5hmC, no MM/ML tags) and crucially does the DISO…
    cite: Lee D, et al. Metheor: Ultrafast DNA methylation heterogeneity calculation from bisulfite read alignments. PLOS Computational Biology 2023;19(3):e1010946. DOI 1…

## CAT: long-read phasing and allele-specific-methylation (ASM) tools — comparison vs ISM (InterSubMod)
- **MethPhaser** [long-read (ONT R9/R10), 5mC CpG via Remora/Dorado;…] gran=read-level assignment operating within SNV-defined phase-blo… ✓
    method: SNV-FIRST then methylation extension. Input = pre-phased SNVs (WhatsHap) + BAM with MM/ML tags. Between two adjacent SNV phase blocks separated by a homozygous gap, it tests whether the two alleles' base-modification probabilities differ, then assigns/votes reads from the gap to a block by which haplotype methylation p…
    stat: Wilcoxon rank-sum test on per-allele base-modification probabilities (haplotype-specific methylation if p<0.05). Read assignment via VOTING (not a probabilistic model): default min…
    hap: yes — its entire purpose is haplotype phasing; methylation is the linkage signal, not the output.
    vs_ISM: OPPOSITE direction of use. MethPhaser CONSUMES methylation to build phase; ISM CONSUMES haplotype (HP tags from LOH-constrained phasing) to MEASURE somatic ASM structure. MethPhaser stat = Wilcoxon on per-allele mod-probability (2-sample location test, germline diploid); ISM = read-to-read DISTANCE …
    cite: Fu Y, Aganezov S, Mahmoud M, Beaulaurier J, Juul S, Treangen TJ, Sedlazeck FJ. MethPhaser: methylation-based long-read haplotype phasing of human genomes. Nat C…
- **NanoMethPhase** [long-read (ONT) only; Python tool + companion SNVo…] gran=region-level (DMR) is the primary ASM output; reads are firs… ✓
    method: PHASE-FIRST then ASM. Phased SNVs + per-read CpG methylation calls are used to assign reads to diploid haplotypes ('phase reads along their CpG methylation to diploid haplotypes'); then differential methylation between the two haplotype read sets is called. SNVoter optionally upgrades low-coverage SNV calls before phas…
    stat: DMR/differential-methylation-analysis via the DSS R/Bioconductor package (Dispersion Shrinkage for Sequencing; beta-binomial / Wald with area statistic across CpGs in a region) — N…
    hap: yes — diploid germline haplotypes (HP1/HP2); designed around het SNVs.
    vs_ISM: Closest archetype to the GERMLINE counterpart of ISM, but three core divergences: (1) Germline imprinting, het-SNV-anchored — ISM is somatic cancer, LOH-constrained (often NO het SNVs, so HP tags come from copy/somatic phasing not germline het). (2) DSS beta-binomial DMR region statistic vs ISM PERM…
    cite: Akbari V, Garant J-M, O'Neill K, Pandoh P, Moore R, Marra MA, Hirst M, Jones SJM. Megabase-scale methylation phasing using nanopore long reads and NanoMethPhase…
- **NANOME** [long-read (ONT R9.4.1/R10.4) only; Nextflow pipeli…] gran=all three: single-molecule/read-level (F1), site-level (MSE)… ✓
    method: Two parts. (a) CONSENSUS CALLER: XGBoost meta-model over Megalodon + Nanopolish + DeepSignal per-CpG predictions + 17-nt sequence feature, inverse-class-frequency weighting toward discordant/non-singleton hard regions. (b) HAPLOTYPE-AWARE ASM: PHASE-FIRST — Clair3 variant calling + long-read phasing, then allele-specif…
    stat: Consensus model: XGBoost (lr=0.1, n_estimators=60, max_depth=9, subsample=0.8; F1=0.926). ASM/DMR: methylKit Chi-squared test, q<0.05, 1000bp sliding window/step. Phasing validated…
    hap: yes — Clair3 + phasing; explicitly markets 'haplotype-aware allele-specific consensus DNAme'. Germline het-SNV anchored.
    vs_ISM: Different layer. NANOME's novelty is the CONSENSUS CALLER (caller-ensembling at signal level — orthogonal to ISM, which consumes already-called MM/ML probabilities from the BAM, no re-calling). Its ASM module is again phase-first germline imprinting with methylKit Chi-square sliding-window DMR — a p…
    cite: Liu Y, Taha HB, Zhang Q, Pan Z, Chatzipantsiou C, Wade E, Slocum T, Karuturi L, Zhao Y, Karmakar S, Li S. NANOME: A Nextflow pipeline for haplotype-aware allele…
- **HapBridge** [long-read (ONT R9/R10 + PacBio HiFi); methylation-…] gran=site-level (HSM sites) + read-level (unphased read tagging) … ✗UNVERIFIED
    method: PHASE-FIRST then methylation refinement, two modules. (1) switch-error-correction: identifies haplotype-specific methylation (HSM) sites and uses their INTER-SITE CONSISTENCY to localize and flip switch errors. (2) block-bridging: iteratively infers haplotype tags for unphased reads from HSM sites, then uses those newl…
    stat: Mechanism = haplotype-specific-methylation-site detection + inter-site consistency scoring + iterative inference (per abstract). EXACT statistical test (e.g. whether HSM detection …
    hap: yes — phasing is the goal; methylation is linkage signal.
    vs_ISM: Same axis as MethPhaser — a phasing IMPROVER that EATS methylation, the inverse of ISM (which eats phasing to MEASURE somatic methylation structure). Differs from MethPhaser by adding switch-error correction + iterative HSM bootstrap and PacBio HiFi support, but still germline, het-SNV-rooted, no no…
    cite: Chen H, Nie F, Luo F, Wang J. HapBridge: A Methylation-Guided Approach for Correcting Switch Errors and Bridging Phased Blocks in Long-Read Phasing. bioRxiv 202…
- **longphase modcall (+ co-phasing)** [long-read (ONT + PacBio HiFi/CCS); C/C++ tool (Lin…] gran=site-level modification calls (modcall.vcf) consumed by hapl… ✓
    method: modcall scans MM/ML-tagged BAM, binarizes per-position modification, and emits sites where the two allele groups differ in methylation into modcall.vcf. Phasing: `phase --mod-file modcall.vcf` jointly phases SNP+INDEL+modification (graph/weighted-edge phasing, same engine that co-phases SNP+SV). v2.0 adds ModCall x SNP…
    stat: THRESHOLD-based, not a hypothesis test: modThreshold=0.8 (call modified if ML>0.8), unModThreshold=0.2 (unmod if <0.2), heterRatio=0.7 (keep sites where mod/unmod ratio looks allel…
    hap: yes — co-phases modifications with SNPs/indels/SVs into haplotype blocks.
    vs_ISM: Notable: longphase modcall's binarization thresholds (mod>0.8, unmod<0.2) are IDENTICAL to ISM's MethylationMatrix binarization (>0.8 meth / <0.2 unmeth) — same upstream convention. But modcall is a phasing-engine site-caller using fixed ratio thresholds and graph max-weight assignment; it has NO st…
    cite: Lin J-H, Chen L-C, Yu S-C, Huang Y-T. LongPhase: an ultra-fast chromosome-scale phasing algorithm for small and large variants. Bioinformatics 2022;38(7):1816-1…

## CAT: Short-read (2nd-gen WGBS/RRBS) and array general DMR/DMC callers — surveyed as the comparison baseline against ISM (ONT …
- **DSS (Dispersion Shrinkage for Sequencing)** [short-read WGBS/RRBS; general/germline (group case…] gran=DML = single CpG; DMR = aggregated multi-CpG region (callDMR… ✓
    method: Beta-binomial model of methylated read counts per CpG. Two-paper lineage: (1) original DSS — arcsine/Wald with a moving-average SMOOTHING step (default 500 bp window) to estimate mean methylation, then empirical-Bayes (log-normal prior) dispersion SHRINKAGE borrowing info from neighboring CpGs as pseudo-replicates, the…
    stat: Beta-binomial; empirical Bayes dispersion shrinkage (log-normal prior); Wald test; arcsine-link beta-binomial GLM (Park&Wu). Spatial CpG correlation handled by 500bp moving-average…
    hap: No — tests between-GROUP mean (condition contrast). No within-sample haplotype axis, no read-to-read structure.
    vs_ISM: DSS aggregates reads into per-CpG counts and tests GROUP MEAN difference (Wald on beta-binomial); ISM keeps the read x CpG matrix and tests whether read GROUPS are STRUCTURALLY separated (read-to-read distance + PERMANOVA pseudo-F). DSS's smoothing handles 1D genomic spatial correlation; ISM's dista…
    cite: Feng, Conneely, Wu 2014 Nucleic Acids Res, PMID 26184873 / DOI 10.1093/nar/gkv715 (DML+smoothing+EB shrinkage+Wald); Wu et al 2015 (no-replicate DMR) PMC4666378…
- **methylKit** [short-read WGBS/RRBS (+ array-compatible counts); …] gran=Per-CpG (per-base) primary; tiled fixed-window regions optio… ✓
    method: calculateDiffMeth: if multiple samples/group -> logistic regression of (methylated C, unmethylated C) on group, tested by Chi-square (default) or F-test; if one sample/group -> Fisher's exact test (fast.fisher or midPval). Optional overdispersion correction overdispersion='MN' (McCullagh & Nelder 1989 scaling) or exper…
    stat: Logistic regression (Chisq/F) or Fisher's exact; overdispersion='none'(default)/'MN'/'shrinkMN'; p-value adjust default SLIM (also BH/qvalue/bonferroni). Does NOT model adjacent-Cp…
    hap: No — group (treatment vs control) contrast only.
    vs_ISM: methylKit's logistic regression is a per-site GROUP-mean test with explicit independence across CpGs (it openly does NOT model spatial correlation) — the opposite of ISM, which is built ON the between-read distance matrix. methylKit covariate adjustment is the analogue of ISM's NormalBaseline residu…
    cite: Akalin et al 2012 Genome Biology 13:R87, PMID 23034086 / DOI 10.1186/gb-2012-13-10-r87; calculateDiffMeth docs rdrr.io/bioc/methylKit
- **metilene** [short-read WGBS/RRBS / any methylation-level input…] gran=DMR (de-novo segmented region) primary; also reports DMC (si… ✓
    method: Fast circular binary segmentation (CBS) on the MEAN methylation DIFFERENCE signal between two groups to define candidate DMR boundaries de novo; after filter steps (min CpG count, min length) each candidate is tested by a two-dimensional Kolmogorov-Smirnov test (2D-KS); output also reports an independent Mann-Whitney U…
    stat: Circular binary segmentation (signal = group mean-difference) + 2D Kolmogorov-Smirnov test (adjusted + unadjusted p) + independent Mann-Whitney U. Spatial CpG correlation handled I…
    hap: No — two-group (or multi-group) contrast on collapsed levels.
    vs_ISM: metilene's CBS segments along the GENOMIC axis using the between-group mean-difference; ISM's hierarchical clustering segments along the MOLECULE axis using read-to-read distance — orthogonal segmentation targets. metilene 2D-KS tests distributional difference between two GROUPS' per-CpG levels; ISM…
    cite: Juhling et al 2016 Genome Research 26(2):256-262, PMID 26631489 / DOI 10.1101/gr.196394.115
- **bsseq / BSmooth** [short-read WGBS (low-coverage friendly); general/g…] gran=Region (DMR = contiguous CpG runs over t-stat threshold). Sm… ✓
    method: BSmooth: local-likelihood SMOOTHING of methylation levels via locfit (bandwidth = max of genomic distance h and a width containing ns methylation loci), giving reliable semi-local estimates from low coverage. Then a signal-to-noise t-statistic = difference of smoothed group means / estimated SD; SDs are themselves smoo…
    stat: Local-likelihood (locfit) smoothing; signal-to-noise t-statistic with smoothed + thresholded SD. Spatial CpG correlation handled directly by SMOOTHING. Needs >=2 replicates per gro…
    hap: No — between-group t-statistic.
    vs_ISM: BSmooth smooths along genomic coordinate to denoise low-coverage GROUP means then takes a t-stat — a 'mean-difference + smoothing' philosophy. ISM does NOT smooth; it preserves per-read binarized states (>0.8 / <0.2) and measures between-molecule distance. BSmooth t-stat is group-level; ISM Delta-be…
    cite: Hansen, Langmead, Irizarry 2012 Genome Biology 13:R83, PMID 23034175 / DOI 10.1186/gb-2012-13-10-r83
- **dmrseq** [short-read WGBS/RRBS; general/germline; region-lev…] gran=Region (collection of neighboring CpGs); the statistic is ex… ✓
    method: Two-stage: (1) candidate DMR detection by segmenting genome into CpG groups with consistent differential-methylation evidence; (2) region statistic from a generalized least squares (GLS) regression with a NESTED AUTOREGRESSIVE (AR-type) correlated error structure on transformed (arcsine) methylation proportions — simul…
    stat: GLS with nested AR(1) correlated errors on arcsine-transformed proportions; region PERMUTATION test (pooled null); BH FDR. Spatial CpG correlation modeled EXPLICITLY inside the GLS…
    hap: No — condition/covariate effect, between groups.
    vs_ISM: dmrseq is the most statistically rigorous short-read analogue and shares ISM's PERMUTATION-test philosophy — but the permutation is over SAMPLE LABELS to get a region p-value, whereas ISM's PERMANOVA permutes read-group labels within ONE sample's distance matrix (999 perms, pseudo-F). dmrseq's AR(1)…
    cite: Korthauer, Chakraborty, Benjamini, Irizarry 2018 Biostatistics 19(3):367-383, PMID 29481604 / DOI 10.1093/biostatistics/kxy007; GitHub kdkorthauer/dmrseq
- **DMRcate** [array (Illumina 450K/EPIC) primary, also WGBS; gen…] gran=Region (DMR); input statistic is per-probe/CpG from limma. ✓
    method: Takes per-CpG/probe differential-methylation statistics from limma's moderated t (uses the SQUARED moderated t, t^2, as an unsigned per-site metric -> agnostic to DM direction). Applies a GAUSSIAN KERNEL to smooth t^2 within a window; kernel scale sigma = lambda/C (default lambda=1000bp, optimal sigma~500bp at C=2). Co…
    stat: limma moderated-t (empirical Bayes) -> Gaussian-kernel smoothing of t^2 -> Satterthwaite scaled chi-square p-value -> BH FDR. Spatial CpG correlation handled by Gaussian KERNEL wit…
    hap: No — limma group/coefficient contrast.
    vs_ISM: DMRcate smooths the per-probe limma t^2 with a Gaussian kernel along genomic coordinate to merge correlated array probes into DMRs — it is array-native and unsigned. ISM operates on ONT long reads with raw 5mC AND 5hmC probabilities (arrays can't separate hydroxymethylation; DMRcate can't either), k…
    cite: Peters et al 2015 Epigenetics & Chromatin 8:6, PMID 25972926 / DOI 10.1186/1756-8935-8-6
- **comb-p** [input-agnostic p-value combiner (any spatially-ord…] gran=Region (enriched runs of low combined p-values). Site-level … ✓
    method: Operates on a BED of (possibly irregularly spaced) per-site p-values produced by ANY upstream test. Pipeline: (1) compute spatial AUTOCORRELATION of p-values across lags; (2) Stouffer-Liptak-Kechris (SLK) sliding-window combination — converts neighboring p-values to z-scores, sums/scales them WEIGHTED by the observed a…
    stat: Stouffer-Liptak (z-sum) with Kechris autocorrelation weighting; BH FDR; Sidak region correction. Spatial CpG correlation handled DIRECTLY via empirical autocorrelation weighting of…
    hap: No — agnostic to biology; just combines correlated p-values along coordinates.
    vs_ISM: comb-p is purely a SPATIAL-CORRELATION p-value aggregator on the genomic axis — it is the most explicit 'handle spatial CpG correlation' tool but does NOT generate methylation statistics itself and never sees reads. ISM generates its own structure statistic (PERMANOVA pseudo-F) from a read-to-read d…
    cite: Pedersen et al 2012 Bioinformatics 28(22):2986-2988, PMID 22954632 / DOI 10.1093/bioinformatics/bts545
- **MOABS** [short-read WGBS/RRBS; general/germline; single-CpG…] gran=Single CpG (DMC) primary; DMR by post-hoc HMM clustering of … ✓
    method: Beta-binomial HIERARCHICAL model: methylated reads ~ Binomial given methylation ratio p; p ~ Beta capturing sampling + biological variation. Empirical-Bayes prior estimated genome-wide (exploits bimodal methylation distribution: most CpGs ~0 or ~1). Outputs CDIF (Credible methylation DIFference) = distance from 0 to th…
    stat: Beta-binomial hierarchical + empirical-Bayes genome-wide prior; CDIF = 95% credible-interval-based effect (not a p-value). Spatial CpG correlation NOT modeled in the per-CpG test; …
    hap: No — between-sample/between-group difference.
    vs_ISM: MOABS shares ISM's EFFECT-SIZE-over-p-value instinct: CDIF (credible-interval lower bound) is conceptually similar to ISM reporting Delta-beta + Cohen d + CI ribbon rather than bare p-values. Both also use a genome/normal-wide prior/baseline (MOABS empirical-Bayes prior vs ISM NormalBaseline residua…
    cite: Sun et al 2014 Genome Biology 15:R38, PMID 24565500 / DOI 10.1186/gb-2014-15-2-r38
- **RnBeads (RnBeads 2.0)** [array (Infinium 27k/450k/EPIC) + WGBS/RRBS/MeDIP/M…] gran=Site (per-CpG/probe) + region (predefined annotation: promot… ✓
    method: Comprehensive analysis suite, not a single test. Site-level differential methylation: hierarchical linear models from limma fitted by EMPIRICAL BAYES on M-values (logit-transformed betas). Combined ranking of each site by THREE criteria — (a) absolute mean methylation difference, (b) quotient/relative ratio of means, (…
    stat: limma hierarchical linear model + empirical Bayes (M-values); max-of-3-ranks combined ranking; region p = Fisher's-method combination of site p-values. Spatial CpG correlation hand…
    hap: No — sample-group contrast.
    vs_ISM: RnBeads is a multi-platform PIPELINE whose differential engine is limma (same family as DMRcate's input) with a max-rank fusion of effect-size + ratio + p-value — a rank-combination philosophy unlike any single ISM statistic. Its region step (Fisher combination of within-region p-values) parallels c…
    cite: Assenov et al 2014 Nature Methods 11(11):1138-1140, PMID 25262207 / DOI 10.1038/nmeth.3115; Muller et al 2019 (RnBeads 2.0) Genome Biology 20:55, DOI 10.1186/s1…

## CAT: Cancer-specific methylation analysis software (array / short-read bisulfite / cfDNA) — surveyed and verified against ONT…
- **ELMER (Enhancer Linking by Methylation/Expression Relationships) v.2** [R/Bioconductor package; Illumina HM450/EPIC array …] gran=Probe-level (array CpG) -> distal regulatory element -> gene… ✓
    method: Reconstructs gene regulatory networks by linking distal (non-promoter) DNA methylation to nearby gene expression, then nominates master-regulator TFs. (1) Identify Differentially Methylated CpGs (DMCs) at distal probes; (2) anti-correlate each DMC's methylation with expression of nearby (default 20 up + 20 downstream) …
    stat: Unsupervised mode: contrasts the n%=20 most extreme-methylation tumors vs rest (Mann-Whitney U / Wilcoxon for probe-gene pairs); Supervised mode: two predefined groups. Motif enric…
    hap: No — no phasing, no haplotype/allele resolution; cohort group contrast only.
    vs_ISM: Opposite axis. ELMER is a COHORT, cross-sample, methylation<->EXPRESSION integration tool needing matched RNA-seq; it answers 'which enhancer methylation changes drive which gene/TF network across a tumor cohort'. ISM is a single-sample, WITHIN-region, read-to-read STRUCTURE tool (PERMANOVA on a rea…
    cite: Silva TC et al., Bioinformatics 2019;35(11):1974-1977. PMID:30364927; DOI:10.1093/bioinformatics/bty902; PMC6546131.
- **MethylMix** [R/Bioconductor package; methylation array + matche…] gran=CpG-level (mapped to closest gene); per-gene mixture-state a… ✓
    method: Identifies hyper- and hypo-methylated genes whose methylation FUNCTIONALLY affects expression. Two-step: (Step 1) per CpG-to-nearest-gene, require methylation->expression effect (transcriptional predictivity filter); (Step 2) univariate BETA MIXTURE MODEL over patients to find discrete methylation states; (Step 3) comp…
    stat: Step 1: linear regression methylation vs expression, P<0.01. Step 2: univariate beta mixture model, number of components chosen by Bayesian Information Criterion (BIC) (add compone…
    hap: No — patient-level mixture states, no within-sample phasing or allele resolution.
    vs_ISM: Different question and granularity. MethylMix clusters PATIENTS into discrete methylation states per gene (beta mixture + BIC) and keeps only methylation that perturbs EXPRESSION (needs matched RNA). ISM clusters READS within one region (UPGMA + silhouette) and never uses expression. MethylMix DM-va…
    cite: Gevaert O et al., Bioinformatics 2015;31(11):1839-1841. DOI:10.1093/bioinformatics/btv020; PMC4443673.
- **conumee** [R/Bioconductor package; Illumina 450K/EPIC methyla…] gran=Bin-level (~50kb) -> segment-level copy-number. Single-sampl… ✓
    method: Repurposes methylation arrays for COPY-NUMBER detection (not methylation calling). (1) Sum methylated + unmethylated channel intensities per probe = total locus DNA abundance; (2) normalize query against a set of copy-neutral normal controls via multiple linear regression (corrects GC/probe-type bias); (3) combine neig…
    stat: Multiple linear regression normalization to control panel; log2 ratio; Circular Binary Segmentation (Olshen et al. 2004, DNAcopy package) for segment calling. No methylation-state …
    hap: No — total intensity per bin; no allelic/haplotype separation (cannot resolve cnLOH allele identity).
    vs_ISM: Orthogonal but COMPLEMENTARY at the concept level. conumee throws AWAY the methylation signal and uses only total array intensity to call CNV segments; ISM keeps the methylation MM/ML signal and explicitly COUPLES ASM to LOH/CN as a covariate (NormalBaseline residual / copy-partition holds CN/ploidy…
    cite: conumee package (Hovestadt V & Zapatka M, DKFZ); method basis Sturm D et al., Cancer Cell 2012;22(4):425-437 (PMID:23079654). conumee Bioconductor: https://bioc…
- **InfiniumPurify** [R/CRAN package; Illumina 450K methylation array; t…] gran=CpG-site level (array probes); per-sample SCALAR purity esti… ✓
    method: Estimates tumor purity from the distribution of beta values at informative differentially-methylated CpGs (iDMCs), then offers purity-aware downstream analysis. getPurity: select iDMCs (large tumor-normal methylation difference + high tumor variance; or load TCGA pre-selected iDMCs by cancer type when no normals), then…
    stat: getPurity: Gaussian kernel density estimation over iDMC beta values (iDMCs selected by tumor-normal difference + tumor variance, with the revision keeping DMPs with tumor variance …
    hap: No — bulk beta values; no allelic or haplotype resolution.
    vs_ISM: Different deliverable. InfiniumPurify outputs one scalar tumor PURITY per sample (kernel density over iDMC betas) and corrects DM tests for it; ISM does not estimate purity — it characterizes within-region read structure and treats subclonal mixing directly via clustering + PERMANOVA. Conceptual lin…
    cite: Zheng X et al., Genome Biology 2017;18:17 (DOI:10.1186/s13059-016-1143-5); R package: Zheng X et al., Genom Data 2018 (PMC6147081). Original method: Zheng et al…
- **methclone** [C++ open-source tool; eRRBS/WGBS bisulfite sequenc…] gran=Read-level, locus = 4 adjacent CpGs; pairwise TWO-sample (e.… ✓
    method: Quantifies COMPOSITIONAL change of clonal epialleles between two samples/stages. For each locus of 4 adjacent CpGs on a read, defines the epiallele as one of 2^4 = 16 methylation patterns; computes combinatorial entropy of the 16-pattern read-count distribution; measures the entropy DIFFERENCE (deltaS) between two stag…
    stat: Combinatorial entropy S = sum of ln(permutations) of the 16-pattern epiallele read distribution; deltaS = S_foreground - S_background, ranging 0 to -144 (more negative = bigger shi…
    hap: No — epiallele = 4-CpG methylation pattern on a read, no SNP/haplotype linkage, no allele resolution.
    vs_ISM: Closest READ-LEVEL relative, but DISORDER family, not structure. methclone measures the combinatorial-entropy SHIFT of 4-CpG epialleles between two samples (clonality dynamics) — exactly ISM's CORE 4 'disorder' contrast (NME / epipolymorphism, 4-CpG window). ISM's primary engine is the opposite: rea…
    cite: Li S, Garrett-Bakelman FE et al., Genome Biology 2014;15(9):472. DOI:10.1186/s13059-014-0472-5; PMC4242486.
- **epihet** [R/Bioconductor package; consumes methclone output …] gran=Read-level (4-CpG epialleles), single-sample per-locus heter… ✓
    method: Computes per-locus intra-tumoral epigenetic-heterogeneity metrics from the same 16 epiallele patterns (methclone v0.2 output), then does differential heterogeneity (DEH) cancer-vs-normal and builds co-heterogeneity networks. Metrics per locus: Proportion of Discordant Reads (PDR), Epipolymorphism (EPM), Shannon entropy…
    stat: PDR = discordant reads / total reads. Epipolymorphism = 1 - sum(p_i^2) over 16 patterns. Shannon entropy = -sum(p_i log p_i). DEH test: t-test (PDR/epipolymorphism, cancer vs norma…
    hap: No — 4-CpG read epialleles only; no SNP phasing or allele separation.
    vs_ISM: Same disorder-family overlap as methclone — epipolymorphism + Shannon entropy over 4-CpG epialleles is EXACTLY ISM's CORE 4 contrast (ISM also computes NME + epipolymorphism on a 4-CpG window as the 'disorder' family it uses to set against its 'structure' engine). Difference: epihet's headline quest…
    cite: Chen X, Ashoor H et al., Scientific Reports 2021;11(1):376 (published 11 Jan 2021). DOI:10.1038/s41598-020-79627-x; PMC7801679.
- **informME** [Pipeline (C++/MATLAB/R, also Julia InformMe.jl); W…] gran=Genomic analysis-region level (fixed analysis regions tiling… ✓
    method: Builds a per-region PROBABILISTIC model of the joint methylation state of neighboring CpGs using the 1D ISING model (statistical physics) realized as an inhomogeneous Markov chain, estimated from WGBS reads (works even at low coverage / single sample because it borrows strength across the region). From the fitted model…
    stat: 1D Ising / Markov-random-field with parameters alpha, beta, gamma estimated from WGBS reads. Single-sample outputs: MML (Mean Methylation Level), NME (Normalized Methylation Entrop…
    hap: No (base informME) — region model not phased. (Separate Ising-based extension, Abante et al. Nat Commun 2020, does haplotype-dependent allel…
    vs_ISM: Most methodologically sophisticated comparator and the deepest conceptual overlap on 'order/structure', but via a model, not clustering. informME models the JOINT distribution of neighboring CpGs (Ising correlations) to get NME/JSD — capturing methylation organization/heterogeneity per region in one…
    cite: Jenkinson G, Abante J, Feinberg AP, Goutsias J, BMC Bioinformatics 2018;19(1):87 (DOI:10.1186/s12859-018-2086-5; PMID:29514626; PMC5842653). Foundational theory…
- **CancerLocator (cfDNA deconvolution)** [Java tool; cfDNA WGBS; non-invasive cancer detecti…] gran=CpG-cluster level; per-plasma-sample scalar tumor burden (th… ✓
    method: Probabilistic deconvolution of plasma cfDNA methylation into a normal-plasma component plus (possibly) a solid-tumor component, simultaneously inferring tumor burden fraction and tissue-of-origin. Group CpGs into clusters; select informative clusters by methylation range across normal plasma + tumor types (MR>=0.25, ~1…
    stat: Per-cluster Beta(alpha,beta) per tumor type / normal plasma (method-of-moments). Mixture x_k=(1-theta)v_k+theta*u_k; observed counts m_k ~ Binomial(n_k, x_k); derived distribution …
    hap: No — bulk cfDNA cluster-level beta values; no phasing or allele resolution.
    vs_ISM: Most distant from ISM. CancerLocator is a COHORT-TRAINED, bulk cfDNA classifier: Beta-distribution deconvolution of plasma methylation into tumor-fraction + tissue label, requiring a reference panel of tumor-type methylation signatures. ISM is reference-light, single-region, read-resolved structure …
    cite: Kang S, Li Q et al., Genome Biology 2017;18:53. DOI:10.1186/s13059-017-1191-5; PMID:28335812; PMC5364586.

## CAT: Methylation-locus association / co-methylation methods (CpG-CpG coupling), surveyed and primary-source-verified, contras…
- **Methylation Haplotype Blocks (MHB) + Methylation Haplotype Load (MHL) — Guo et al. 2017** [Method/metric (read-level co-methylation), short-r…] gran=READ-LEVEL co-methylation (each read = one methylation haplo… ✓
    method: MHB: scan adjacent CpGs and merge into a block while linkage-disequilibrium r^2 of methylation between NEIGHBORING CpGs stays above threshold. r^2 computed from a 2x2 contingency of joint methylation states across INDIVIDUAL READS (N00/N01/N10/N11 = unmeth-both/meth-2nd-only/meth-1st-only/meth-both), like genetic LD bu…
    stat: Signed/linkage r^2 of methylation between adjacent CpGs (merge criterion); MHL = Σ_l (w_l·P_l)/Σ_l w_l, P_l = fraction fully-methylated substrings length l. mHapTk defaults: MHB r^…
    hap: No (methylation-only; no SNV/HP partition). Cancer use = cfDNA tissue-of-origin deconvolution, NOT allelic/somatic ASM, no 5hmC.
    vs_ISM: Closest conceptual cousin but ORTHOGONAL axis. MHB/MHL quantify WITHIN-MOLECULE coupling ALONG the genome (CpG_i co-varies with CpG_i+1 across reads -> LD r^2) and collapse a region to a coordination scalar. ISM instead builds a READ-TO-READ distance matrix (NHD/L1/Bernoulli/Pearson/Jaccard over rea…
    cite: Guo S, et al. Nat Genet. 2017;49(4):635-642. PMID 28263317; DOI 10.1038/ng.3805
- **mHapTk (mHap toolkit — implementation of read-level co-methylation: R2, MHBDiscovery, MHL)** [Software toolkit (Python), read-level, short-read …] gran=READ-LEVEL: docs state co-methylation of pairwise CpGs is 'c… ✓
    method: Operates on mHap format (each line = read-level methylation haplotype across CpGs, from BAM via mhapTools). Sub-commands: R2 (pairwise co-methylation LD r^2 from individual reads, signed, p-values from 2x2 N00/N01/N10/N11), MHBDiscovery (greedy block extension while adjacent-CpG r^2 significant), stat/genomeWide (8 sta…
    stat: Signed r^2 + p per CpG pair (2x2). MHBDiscovery defaults r^2>=0.5, p<=0.05, window 5. MHL minK=1/maxK=10. PDR/entropy/MBS/MCR/CHALM additional read-level stats.
    hap: No.
    vs_ISM: mHapTk's R2 is the most direct external analog to ISM's CpG-coupling intuition, but it measures CpG-PAIR LD (column-column) whereas ISM measures READ-READ distance (row-row). Both read the same read x CpG matrix ISM builds; divergence is the reduction operator: mHapTk -> per-CpG-pair r^2 + region sc…
    cite: Ding J, et al. Bioinformatics. 2022;38(22):5141-5143. PMID 36179079; DOI 10.1093/bioinformatics/btac650. [Defaults VERIFIED from GitHub README]
- **Co-methylation correlation / LD-of-methylation (the general CpG-CpG primitive)** [Statistical primitive (not one tool): cohort-level…] gran=(a) cohort/cross-sample; (b) read/within-molecule. r or r^2 … ✓
    method: Two conflated flavors: (a) COHORT-level = Pearson/Spearman correlation of a CpG/region methylation ACROSS INDIVIDUALS/SAMPLES (CoMeBack, Comethyl, coMET) — needs N samples, population covariation; (b) READ-level = LD r^2 of joint methylation states across READS within one sample (MHB/mHapTk) — within-molecule physical …
    stat: Pearson/Spearman r (cohort); LD r^2 + p from 2x2 (read). Thresholds context-dependent (CoMeBack 15-30%; MHB r^2>=0.5).
    hap: No (generic).
    vs_ISM: Fixes the key axis distinction for ISM positioning: ISM is single-sample, read-level, but measures READ-READ distance not CpG-CpG correlation. Cohort co-methylation (a) is impossible on ISM's single-tumor design. Read-level LD (b) is the column-wise dual of ISM's matrix. CRUCIAL: ISM's Pearson is ap…
    cite: Concept; read-level LD formalized in Shoemaker R, et al. Genome Res. 2010;20(7):883-889; and Guo 2017 (MHB). [cohort-vs-read distinction VERIFIED across CoMeBac…
- **CoMeBack (Co-Methylation with genomic CpG Background)** [R/Bioconductor package, COHORT-level, Illumina arr…] gran=COHORT/cross-individual: correlation of probe beta ACROSS PE… ✓
    method: CMRs = clusters of adjacent array probes meeting: (i) inter-probe distance <2 kb; (ii) unmeasured genomic CpG 'chain' density >=1 CpG per 400 bp between probes (the genomic-background filter for spurious long-range correlation); (iii) cross-individual DNAm correlation between adjacent probes above threshold. No phenoty…
    stat: User-selectable Pearson/Spearman/Kendall; guidance N>=500 with corr>=15%; reference whole-blood CMRs at >=30% cutoff; distance<2 kb; background >=1 CpG/400 bp.
    hap: No.
    vs_ISM: Maximally distant from ISM on every axis: array (not BAM/ONT), cohort-correlation (not single-sample read geometry), region-of-probes (not reads), germline-cohort (not tumor-somatic), no haplotype/allele, no 5hmC, no normal-anchored cis-test. Useful only as a contrast for what 'co-methylated region'…
    cite: Gatev E, Gladish N, Mostafavi S, Kobor MS. Bioinformatics. 2020;36(9):2675-2683. DOI 10.1093/bioinformatics/btaa049. [thresholds VERIFIED from full text]
- **Comethyl (WGCNA region co-methylation network — Mordaunt et al.)** [R package, COHORT-level network, short-read WGBS —…] gran=COHORT/cross-sample network: correlation of REGION methylati… ✓
    method: Extends WGCNA: (1) regions = CpG clusters (>=3 CpGs within 150 bp) or annotations; (2) per-region percent methylation per sample; (3) WGCNA signed adjacency a_ij=|(1+cor(x_i,x_j))/2|^beta (Pearson across SAMPLES, soft-threshold power beta); (4) topological overlap matrix (TOM) -> hierarchical clustering into modules; (…
    stat: Signed adjacency ((1+Pearson r)/2)^beta; TOM; hierarchical clustering; eigennode=PC1; eigennode-trait correlation. Low-pass WGBS (3-8x).
    hap: No.
    vs_ISM: Shares ISM's hierarchical-clustering machinery but on the OPPOSITE matrix orientation and scale: Comethyl clusters REGIONS using cross-SAMPLE correlation into modules (needs a cohort); ISM clusters READS within ONE region by read-read distance into subclonal groups. Comethyl's eigennode (PC1 of regi…
    cite: Mordaunt CE, Mouat JS, Schmidt RJ, LaSalle JM. Brief Bioinform. 2022;23(2):bbab554. PMID 35037016; DOI 10.1093/bib/bbab554. [adjacency formula + eigennode VERIF…
- **CGmapTools (ASM mode 'asr')** [Toolkit (C/Python), READ-level allele-partitioned …] gran=READ-LEVEL, allele(SNV)-partitioned: reads split by het-SNV … ✓
    method: Call het-SNVs (BayesWC/BinomWC, precision ~80%->99%), then for regions LINKED by a het-SNV through mapped reads, partition reads by allele and compare methylation between the two alleles. ASM call requires: hypomethylated allele mCG<=0.2 AND hypermethylated allele >=0.8, with BH-corrected P (multiple t-tests) <=0.05. T…
    stat: t-tests on per-allele methylation (BH P<=0.05) + thresholds mCG<=0.2 / >=0.8 on the two alleles.
    hap: Single-SNV allelic only (not multi-SNV phased haplotype; no normal anchor).
    vs_ISM: Most similar in SPIRIT to ISM's ALLELE-axis (ALT vs REF) but mechanically simpler and germline-framed: CGmapTools splits reads by a het-SNV allele and t-tests mean methylation; NOT phase-aware beyond one SNV, NO normal baseline, NO somatic-controlled HP1-vs-HP1-1 axis, NO PERMANOVA/clustering, NO co…
    cite: Guo W, et al. Bioinformatics. 2018;34(3):381-387. PMID 28968643; DOI 10.1093/bioinformatics/btx595. [t-test + 0.2/0.8 thresholds VERIFIED from PMC6454434]
- **eDMR (empirical Differentially Methylated Regions; methylKit extension)** [R package, group-comparison DMR caller, short-read…] gran=Region-level DMR between GROUPS (case vs control); spatial p… ✓
    method: (1) Model genome-wide distribution of distances between differentially methylated CpGs with a BIMODAL mixture to set optimal region-segmentation distance cutoff; (2) group adjacent significant CpGs into candidate regions; (3) combine per-CpG p-values within a region via a WEIGHTED Z-test (Stouffer-style) with a weighte…
    stat: Bimodal-distribution distance threshold for segmentation; weighted Z-test (combined p) per region; methylKit per-CpG differential tests upstream.
    hap: No.
    vs_ISM: Category mismatch — included per user list but it is a DMR caller (where methylation DIFFERS between groups), not a methylation-locus ASSOCIATION/co-methylation method. No CpG-CpG coupling, no read-read distance, no within-molecule structure. ISM's per-CpG Fisher+BH-FDR (CORE 4) is the nearest ISM c…
    cite: Li S, Garrett-Bakelman FE, Akalin A, et al. BMC Bioinformatics. 2013;14(Suppl 5):S10. PMID 23735126. [algorithm VERIFIED at search/abstract level; full Methods …
- **Metheor (LPMD / PDR + 5 heterogeneity measures)** [Software (Rust), READ-level within-sample heteroge…] gran=READ-level / read-pair / CpG-pair-within-read. LPMD = CpG-pa… ✓
    method: Computes 7 read-level measures from bisulfite alignments: PDR (proportion of discordant reads — a read is discordant if inconsistent across its CpGs, Landau 2014), LPMD (Local Pairwise Methylation Discordance — fraction of CpG PAIRS at a FIXED genomic distance d that are discordant, removing PDR's read-length bias; def…
    stat: PDR = fraction discordant reads; LPMD = mean discordance over CpG pairs at distance d (removes read-length scaling); epipolymorphism/entropy on 4-CpG epialleles.
    hap: No.
    vs_ISM: LPMD is the cleanest external analog to a read-level CpG-CpG dissimilarity, and PDR/epipolymorphism/entropy directly mirror ISM CORE 4 disorder family (NME, epipolymorphism, 4-CpG window). KEY contrast: Metheor reduces a locus to a DISORDER scalar (how messy), discarding read identity/structure; ISM…
    cite: Lee D, et al. PLoS Comput Biol. 2023;19(3):e1010946. PMID 36940213; DOI 10.1371/journal.pcbi.1010946. [LPMD vs PDR VERIFIED from PMC10062925]
- **FDRP / qFDRP + Epipolymorphism + Methylation Entropy (WSH score family — Scherer NAR 2020; Landan 2012; Xie 2011)** [Read-pair / epiallele-level within-sample heteroge…] gran=FDRP/qFDRP = READ-PAIR level (compares two reads via Hamming… ✓
    method: FDRP: for a CpG, over all PAIRS of reads covering it, fraction whose methylation patterns disagree across overlapping CpGs (binary). qFDRP: same but normalized HAMMING distance between the two reads' methylation vectors (quantitative). Epipolymorphism (Landan 2012): probability two random molecules differ over a 4-CpG …
    stat: FDRP = fraction discordant read pairs; qFDRP = mean normalized Hamming distance over read pairs; epipolymorphism = 1-Σ p_i^2; ME = -Σ p_i log2 p_i / N_CpG.
    hap: No (no SNV/HP partition).
    vs_ISM: qFDRP is the SINGLE closest statistical relative to ISM's core: both compute a normalized Hamming/dissimilarity between two reads' methylation vectors. Decisive difference is the REDUCTION: qFDRP averages all pairwise read distances into ONE disorder scalar per CpG (discards geometry); ISM KEEPS the…
    cite: Scherer M, et al. Nucleic Acids Res. 2020;48(8):e46. DOI 10.1093/nar/gkaa120 (FDRP/qFDRP); Landan G, et al. Nat Genet. 2012;44(11):1207-1214 (epipolymorphism); …

## CAT: Allele-specific methylation (ASM) callers — survey vs ISM (InterSubMod, ONT long-read cancer somatic, read-to-read dista…
- **DAMEfinder** [R/Bioconductor package; short-read bisulfite (BS-s…] gran=Per-CpG (SNP mode) / per-CpG-tuple (tuple mode) ASM scores a… ✓
    method: Two modes. (1) SNP-based 'exhaustive' mode: needs a VCF of HETEROZYGOUS SNPs (called by Bis-SNP from the BS-seq itself), assigns each overlapping read to reference vs alternative allele, computes per-SNP ASM = |X_M^ref/X^ref - X_M^alt/X^alt| (difference in methylation proportion between the two allele groups). (2) Tupl…
    stat: Differential ASM between conditions: per-position limma lmFit + eBayes moderated t-statistic on the ASM scores; spatial smoothing via bumphunter clusterMaker + loessByCluster; regi…
    hap: SNP-mode is single-het-SNP-anchored (unphased — does NOT require phased haplotypes); tuple-mode is SNP-free and read-pattern-based. No long-…
    vs_ISM: Closest in spirit to ISM's ALLELE-axis (ALT vs REF read methylation) and its per-CpG Fisher/Δβ 'disorder' family. KEY DIFFERENCES: DAMEfinder is short-read BS-seq germline/imprinting-oriented; its tuple ASM score = log-OR of pairwise CpG concordance (MM·UU/MU·UM) — a 2-CpG co-methylation metric, NOT…
    cite: Orjuela S, Machlab D, Menigatti M, Marra G, Robinson MD. Epigenetics & Chromatin 2020;13:25. DOI:10.1186/s13072-020-00346-8; PMC7268773; PMID 32487212
- **methylpy AMRfinder (allelically methylated regions)** [Python pipeline (methylpy / MethPipe lineage); sho…] gran=Per-read methylation pattern within a window, aggregated to … ✗UNVERIFIED
    method: AMRfinder (methylpy 'allelic' / call-AMR) detects ALLELE-SPECIFIC methylation at REGION level from BS-seq WITHOUT requiring SNP information. It models per-read CpG methylation patterns and tests whether reads in a window are better explained by a single methylation distribution vs TWO distinct allelic methylation state…
    stat: Likelihood-ratio framework: compares a single-allele (one parameter) model against a two-allele model for reads in a window; significant windows merged into AMRs. Lineage of Fang 2…
    hap: SNP-free and phasing-free — infers two allelic epialleles from read methylation patterns directly.
    vs_ISM: Conceptually the SAME family as ISM's core idea (read methylation patterns reveal two molecular populations WITHOUT SNPs). BUT methylpy AMRfinder reduces this to a 1-vs-2-distribution LR test on a window and outputs a binary AMR region; ISM keeps the full read×read distance matrix + hierarchical clu…
    cite: Schultz MD et al. (methylpy, Nature 2015, 'Human body epigenome maps...') / Fang F et al. PNAS 2012 (AMR LR model lineage). https://github.com/yupenghe/methylpy
- **Fang et al. 2012 per-read ASM Fisher exact (foundational method)** [Statistical method / pipeline; short-read WGBS (Me…] gran=Per-read CpG calls on SNP-containing reads -> per-SNP/per-Cp… ✓
    method: The canonical READ-LEVEL ASM test: for each heterozygous SNP, separate the reads carrying allele1 vs allele2, then for nearby CpGs build a 2x2 contingency table of (allele) x (methylated/unmethylated CpG read counts) and apply FISHER'S EXACT TEST for association between allele identity and methylation state. Reads are …
    stat: Fisher's exact test on the 2x2 allele x methylation read-count table (association of methylation with one SNP allele); multiple-testing correction across loci.
    hap: Single-het-SNP-anchored, unphased — read assigned to an allele by the SNP base it directly covers.
    vs_ISM: The ancestral 'per-read ASM Fisher' the prompt references, and the direct precedent for ISM's CORE 4 per-CpG Fisher-exact + BH-FDR and its ALLELE-axis. ISM goes beyond it: (a) needs no het SNP for its read×read distance/PERMANOVA structure engine, and (b) adds a somatic-controlled HP-axis (HP1 vs HP…
    cite: Fang F, Hodges E, Molaro A, Dean M, Hannon GJ, Smith AD. PNAS 2012;109(19):7332-7. DOI:10.1073/pnas.1201310109; PMID 22523239
- **MethSCAn (formerly scbs)** [Python tool; SINGLE-CELL short-read bisulfite (scB…] gran=Per-cell methylation residuals -> region (window/VMR) level;… ✓
    method: NOT allele-specific. Identifies Variably Methylated Regions (VMRs) by CELL-to-CELL variance, not allelic variance: divide chromosomes into overlapping windows, compute each cell's deviation (residual) from the smoothed ensemble-average methylation, take the VARIANCE of these residuals per window, select top ~2% highest…
    stat: VMR = variance of per-cell smoothed methylation residuals (top 2% windows). DMR test = Welch's t-statistic on shrunken residuals between cell groups + permutation-based FDR.
    hap: No haplotype/allele handling — when reads disagree at a position it sets NA / majority-call; variance is across CELLS, not alleles.
    vs_ISM: Included because the prompt listed it, but it is NOT an ASM caller — its 'variance/structure' is between CELLS (single-cell), whereas ISM's structure is between MOLECULES/READS within one bulk sample. Shared idea: 'methylation variance reveals structure'; ISM uses read×read distance + PERMANOVA with…
    cite: Kremer LPM et al. Nature Methods 2024;21(9):1616-1623. DOI:10.1038/s41592-024-02347-x; PMC11399085; PMID 39085432. (Earlier as scbs: bioRxiv 2022.06.15.496318)
- **MethGo** [Python post-alignment toolkit; short-read WGBS/RRB…] gran=Per-cytosine and per-region descriptive profiling; SNP/CNV r… ✓
    method: NOT an ASM tool. 5 modules: COV (coverage), MET (methylation profiling), TXN (TFBS methylation), SNP (SNP calling), CNV (CNV calling). The SNP module merely calls homozygous/heterozygous SNPs into a table — COMPLETELY SEPARATE from methylation and does NOT link SNP alleles to CpG methylation status. No allele-specific …
    stat: None for ASM — descriptive distributions/plots only; SNP module is genotype calling, no allele x methylation statistic.
    hap: No — SNP calls not linked to methylation, no phasing, no allele partitioning of CpGs.
    vs_ISM: Listed by the prompt but verified to NOT perform allele-specific methylation — it co-reports SNPs and methylation separately. ISM structurally couples haplotype/allele to per-CpG methylation (HP-axis, ALLELE-axis, Cramér V HP-vs-methylation, normal-anchored cis-test). MethGo is a descriptive QC/prof…
    cite: Liao WW, Yen MR, Ju E, Hsu FM, Lam L, Chen PY. BMC Genomics 2015;16(Suppl 12):S11. DOI:10.1186/1471-2164-16-S12-S11; PMC4682368; PMID 26680022
- **ampliMethProfiler** [Python pipeline; TARGETED deep bisulfite amplicon …] gran=Per-READ epiallele (full CpG methylation pattern of each rea… ✓
    method: EPIALLELE-based (NOT SNP/allele-specific). For a targeted amplicon, each read's binary CpG methylation pattern = an 'epiallele' (epihaplotype). Quantifies per-CpG methylation %, then treats the set of distinct epialleles like an ecological community and computes microbiome-style alpha-diversity metrics. Does NOT use SN…
    stat: Alpha-diversity over epialleles: Shannon entropy, Simpson index, Chao1, observed species, singletons (via QIIME). Read demultiplex/QC via BLAST. This is the epipolymorphism/methyla…
    hap: No SNP, no phasing — purely read methylation pattern (epiallele) diversity.
    vs_ISM: Maps onto ISM's CORE 4 'disorder' family (NME normalized methylation entropy + epipolymorphism over 4-CpG windows). BUT ampliMethProfiler stops at read-pattern DIVERSITY/entropy summaries (Shannon/Simpson) with ecological framing; NO read×read distance matrix, NO clustering into subclones, NO PERMAN…
    cite: Scala G, Affinito O, Palumbo D, Florio E, Monticelli A, Miele G, Chiariotti L, Cocozza S. BMC Bioinformatics 2016;17:484. DOI:10.1186/s12859-016-1380-3; PMC5123…
- **NanoMethPhase** [Python tool; ONT LONG-READ nanopore; read-level ha…] gran=Read-level haplotype assignment (HP1/HP2), aggregated to DMR… ✓
    method: Phases 5mC to two haplotypes from nanopore long reads, then calls allele-specific (haplotype-specific) methylation. REQUIRES a PHASED VCF: SNVs called by Clair, phased by WhatsHap; NanoMethPhase assigns each read to HP1/HP2 using >=2 phased SNVs per read, haplotype ratio >=0.75, base quality >=7. After haplotagging, th…
    stat: Differential methylation HP1 vs HP2 via DSS R package (beta-binomial dispersion-shrinkage; single-CpG Wald test on beta-binomial regression with arcsine link, smoothing for regions…
    hap: Yes — explicitly haplotype-phased (needs external phased VCF from WhatsHap). The long-read GERMLINE-phasing analog of ISM's HP axis.
    vs_ISM: The most directly comparable LONG-READ ASM tool to ISM's HP axis — both haplotag nanopore reads and compare HP1 vs HP2 methylation. CRITICAL DIFFERENCES: (1) NanoMethPhase requires germline-phased SNVs and tests HP1 vs HP2 (germline imprinting); ISM adds a SOMATIC-controlled HP1 vs HP1-1 axis + norm…
    cite: Akbari V, Garant JM, O'Neill K, et al; Jones SJM. Genome Biology 2021;22:68. DOI:10.1186/s13059-021-02283-5; PMC7898412
- **modkit (Oxford Nanopore) dmr** [Rust tool, ONT official; LONG-READ nanopore modBAM…] gran=Region-level (BED) or single-base; operates on AGGREGATED pi… ✓
    method: Official ONT modBAM toolkit. modkit pileup -> bedMethyl; modkit dmr pair/multi compares two (or more) conditions, which CAN be HAPLOTYPE-PARTITIONED bedMethyl tables (modkit pileup --partition-tag HP) -> allele/haplotype-specific methylation when fed phased/haplotagged input. The DMR test is a Bayesian model-comparison…
    stat: log marginal likelihood ratio: score = log[ l_theta_a(Xa) * l_theta_b(Xb) / l_theta_a+b(X_a+b) ] (two conditions modeled separately vs jointly). Binary case: Beta-Binomial conjugat…
    hap: Yes via --partition-tag HP on haplotagged reads — but requires external phasing/haplotagging (e.g. WhatsHap/longphase); modkit itself does n…
    vs_ISM: The strongest production-grade long-read baseline to cite. modkit dmr is a Beta-Binomial/Dirichlet-Multinomial marginal-likelihood-ratio DMR test on AGGREGATED pileup counts — it compares MEAN methylation between two (optionally haplotype-partitioned) groups, conceptually ISM's Δβ axis done Bayesian…
    cite: Oxford Nanopore Technologies, modkit (no formal paper; docs are primary source).
- **ASMS (Raineri, 2024)** [Tool (GitHub ecmra/asms); ONT LONG-READ modBAM; re…] gran=Read-level (per-read methylation pattern) clustered into 2 g… ✗UNVERIFIED
    method: Detects allele-specific methylation WITHOUT phasing. Uses an EM algorithm to cluster long reads at a locus into 2 groups based on the methylation patterns on the reads themselves (a 2-component mixture over per-read binary CpG patterns). Optional scan-vcf mode uses known variants only to nominate candidate loci where m…
    stat: EM-based 2-component clustering of reads by methylation pattern (--maxit iterations, default 10); exact 1-vs-2-component likelihood-ratio decision statistic NOT documented in the r…
    hap: Phasing-free; can ingest a VCF only to nominate candidate loci, then clusters reads by methylation alone. Reads carry MM/ML mod tags.
    vs_ISM: The single most ISM-like external tool: long-read, MM/ML 5mC tags, NO phasing, clusters reads by methylation pattern to find two molecular populations — directly parallels ISM's read-grouping idea. DIFFERENCES: ASMS fixes the model at EM 2-component mixture (two alleles), whereas ISM (a) builds an e…
    cite: Raineri E (CNAG). 'ASMS: finding allele specific methylation in human genomes without phasing.' bioRxiv 2024.12.18.629129 (2024). DOI:10.1101/2024.12.18.629129
- **DSS (beta-binomial engine under NanoMethPhase & many per-read ASM pipelines)** [R/Bioconductor; BS-seq AND nanopore (via NanoMethP…] gran=Per-CpG count test -> region (DMR). ✓
    method: Not an ASM caller per se but the differential-methylation STATISTIC many ASM tools delegate to (incl. NanoMethPhase HP1-vs-HP2). Models methylated/total counts with a beta-binomial; dispersion shrinkage (Bayesian hierarchical) stabilizes per-CpG variance; tests per-CpG difference with a Wald test on a beta-binomial reg…
    stat: Beta-binomial regression, Wald test, arcsine link; dispersion shrinkage via Bayesian hierarchical model; BH/region smoothing.
    hap: Agnostic — operates on whatever two groups it's given (e.g. HP1 vs HP2 counts from NanoMethPhase, or ALT vs REF). Does not phase.
    vs_ISM: The 'beta-binomial test' branch the prompt listed. A MEAN-difference per-CpG model = ISM's Δβ (HP1 vs HP1-1 / ALT vs REF) done with shrinkage + arcsine Wald instead of Wilcoxon signed-rank. NO concept of read×read distance, subclonal clustering, or PERMANOVA structure. ISM could swap its Δβ Wilcoxon…
    cite: Feng H, Conneely KN, Wu H. Nucleic Acids Res 2014;42(8):e69 (DSS); Park Y, Wu H. Bioinformatics 2016;32(10):1446 (general design). Bioconductor: DSS.

## CAT: Within-Sample Heterogeneity (WSH) / epigenetic disorder metrics — survey for contrast against ISM (InterSubMod). VERIFIC…
- **PDR (Proportion / Percent of Discordant Reads) — Landau 2014** [WSH disorder metric, INTRA-molecule (within-read);…] gran=Read-classified, locus-level scalar; NOT a between-read stru… ✓
    method: DISORDER QUANTITY = within-read state mixing. Per locus, classify each read covering >=4 CpGs as CONCORDANT (all CpGs same state) or DISCORDANT (contains BOTH a methylated and an unmethylated CpG on the same single molecule). PDR(locus) = (# discordant reads)/(# reads covering locus). One scalar 'disorder' per locus. N…
    stat: Simple proportion (no permutation, no distance matrix). Original CLL result: mean PDR 24.7% (CLL) vs 17.6% (normal B) / 16.3% (healthy tissues), P<0.00001 (n=104 CLL, 27 normal B).…
    vs_ISM: DISORDER vs STRUCTURE. PDR collapses each read to one concordant/discordant bit -> one scalar/locus. ISM keeps the full read x CpG matrix, computes read-to-read pairwise distances (NHD/L1/Bernoulli/Pearson/Jaccard, min 5 common CpGs), clusters (UPGMA/Ward + silhouette), and runs PERMANOVA (pseudo-F,…
    cite: Landau DA et al. Cancer Cell 2014;26(6):813-825. PMID 25490447. DOI 10.1016/j.ccell.2014.10.012
- **Epipolymorphism — Landan 2012** [WSH disorder metric, INTER-molecule pattern varian…] gran=4-CpG window locus-level scalar; from reads spanning all 4 C… ✓
    method: DISORDER QUANTITY = diversity of co-existing methylation patterns. In a 4-CpG window each read = one of 2^4=16 epialleles; estimate epiallele frequencies p_k; epipolymorphism = 1 - SUM(p_k^2) (probability two random molecules differ; Simpson/Gini-type diversity). Higher = more distinct coexisting patterns. NOT haplotyp…
    stat: Simpson-diversity 1 - SUM(p_k^2); no clustering, no PERMANOVA, no between-read geometry. COVERAGE: hard requirement each read covers all 4 adjacent CpGs; in Scherer NAR 2020 benchm…
    vs_ISM: Disorder-diversity scalar on a fixed tiny 4-CpG window vs ISM's full-region read x CpG matrix + structural clustering + PERMANOVA. Epipolymorphism measures HOW MANY distinct patterns coexist, not whether patterns SEPARATE BY HAPLOTYPE/somatic group. ISM computes epipolymorphism internally (CORE 4) p…
    cite: Landan G et al. Nat Genet 2012;44(11):1207-1214. DOI 10.1038/ng.2442 (formula 1-SUM p_k^2 confirmed via Scherer NAR 2020 re-derivation; primary full text paywal…
- **Methylation Entropy (epiallele Shannon entropy) — Xie 2011 / Landan 2012** [WSH disorder metric, INTER-molecule; SHORT-READ bi…] gran=4-CpG window locus-level scalar. ✓
    method: DISORDER QUANTITY = randomness of epiallele distribution. Shannon entropy of the 16 epiallele frequencies in a 4-CpG window: Entropy = -(1/4) SUM(p_k log2 p_k), normalized so max=1 when all 16 patterns equiprobable. NOT haplotype-aware.
    stat: Normalized Shannon entropy; no structural/permutation test. COVERAGE: same 4-CpGs-per-read requirement and same ~70/1000-region computability limit as epipolymorphism (Scherer); th…
    vs_ISM: Same disorder-vs-structure distinction as epipolymorphism. ISM computes NME/entropy internally (CORE 4) for contrast, but its discriminating engine is the read x read distance + PERMANOVA structure test, not an entropy scalar.
    cite: Xie H et al. Nucleic Acids Res 2011;39(10):4099-4108 (methylation entropy); with Landan G et al. Nat Genet 2012;44(11):1207-1214 DOI 10.1038/ng.2442
- **FDRP (Fraction of Discordant Read Pairs) — Scherer 2020** [WSH disorder metric, INTER-molecule pairwise; SHOR…] gran=Per-CpG scalar (read-pair based). ✓
    method: DISORDER QUANTITY = per-CpG pairwise discordance (binary). For target CpG c, take all PAIRS of reads covering c, compare methylation states over their overlap; pair is discordant if any overlapped CpG differs. FDRP(c) = (# discordant pairs)/(total pairs). NOT haplotype-aware.
    stat: Proportion over read-pair combinations; subsamples 40 reads above 40x to avoid combinatorial explosion; discards overlaps <35 bp. COVERAGE: computed at sites with coverage >=10; FD…
    vs_ISM: Pairwise BINARY discordance collapsed to a per-CpG scalar vs ISM's quantitative read-to-read distance matrix retained as geometry for clustering + PERMANOVA. FDRP asks 'do pairs disagree?'; ISM asks 'do reads partition into structurally separated haplotype groups?' (conditioned on HP/somatic labels …
    cite: Scherer M et al. Nucleic Acids Res 2020;48(8):e46. DOI 10.1093/nar/gkaa120. PMID 32338758
- **qFDRP (Quantitative FDRP) — Scherer 2020** [WSH disorder metric, INTER-molecule pairwise quant…] gran=Per-CpG scalar. ✓
    method: DISORDER QUANTITY = average normalized Hamming dissimilarity among molecule pairs at a CpG (continuous 0-1). Soft FDRP: replaces binary concordance with normalized Hamming distance over read-pair overlap, averaged across all pairs covering c. Less sensitive to single-CpG fluctuations. NOT haplotype-aware.
    stat: Mean normalized Hamming distance; subsample 40 reads above 40x; overlap >=35 bp; min coverage 10. COVERAGE: more coverage-STABLE than PDR/FDRP (benchmark strength).
    vs_ISM: Uses the SAME normalized-Hamming pairwise read kernel as ISM's NHD metric, but averages to a per-CpG SCALAR and discards geometry. ISM keeps the full distance matrix for hierarchical clustering + PERMANOVA structural testing. qFDRP is the closest disorder cousin to ISM's distance kernel: same pairwi…
    cite: Scherer M et al. Nucleic Acids Res 2020;48(8):e46. DOI 10.1093/nar/gkaa120. PMID 32338758
- **MHL (Methylation Haplotype Load) — Guo 2017** [WSH disorder metric, INTRA-molecule co-methylation…] gran=Locus/block-level scalar from reads. ✓
    method: DISORDER QUANTITY (inverse): degree of co-methylation block linkage on a single molecule. MHL = SUM_L w_L * (fraction of length-L substrings fully methylated) / SUM w_L; rewards long consecutively-methylated stretches. 'Haplotype' here = methylation-block linkage on a read, NOT genetic haplotype; not HP-tag aware.
    stat: Length-weighted substring fraction; no permutation test. COVERAGE: read-length dependent; coverage-STABLE in NAR 2020 (strength); re-implementations use min-depth ~10.
    vs_ISM: MHL measures linear co-methylation runs WITHIN a molecule (1D block ordering) vs ISM's BETWEEN-molecule structural partition. ISM's 'haplotype' is the genetic HP1/HP1-1 / ALT-vs-REF axis with somatic-controlled cis-tests; MHL's 'haplotype' is mere methylation-block linkage with no allele concept.
    cite: Guo S et al. Nat Genet 2017;49(4):635-642. DOI 10.1038/ng.3805. PMID 28263317 (re-implemented in Scherer NAR 2020)
- **WSHPackage (FDRP/qFDRP/PDR/Epipolymorphism/Entropy/MHL) — Scherer 2020 implementation + benchmark** [Software battery (R); SHORT-READ bisulfite BAM; th…] gran=Per-CpG (FDRP/qFDRP/PDR) or 4-CpG window (epipoly/entropy) s… ✓
    method: DISORDER QUANTITY = battery spanning intra-molecule (PDR, MHL) and inter-molecule (FDRP, qFDRP, epipolymorphism, entropy) disorder. Computes all six scores from aligned bisulfite reads; the NAR 2020 paper benchmarks them for coverage robustness, single-CpG-error sensitivity, and global-vs-local heterogeneity capture.
    stat: Each score as above; benchmark on 1000 regions + simulated coverage/error sweeps. COVERAGE headline: PDR & FDRP increase with coverage; qFDRP & MHL most coverage-consistent; epipol…
    vs_ISM: This package IS the canonical 'disorder family' ISM contrasts against. None of its six scores build a read x read distance matrix, cluster reads into subgroups, or run a PERMANOVA between-group test; all reduce to per-CpG/per-window scalars. ISM's CORE 3 (PERMANOVA on the distance matrix) is the str…
    cite: Scherer M et al. Nucleic Acids Res 2020;48(8):e46. DOI 10.1093/nar/gkaa120. PMID 32338758. Code: github.com/MPIIComputationalEpigenetics/WSHPackage
- **informME (NME / dNME, information-theoretic Ising) — Jenkinson/Goutsias 2017-2018** [Information-theoretic WSH; SHORT-READ WGBS; region…] gran=Region-level (150-bp units within 3-kb estimation windows); … ✓
    method: DISORDER QUANTITY = region-level methylation entropy from a probabilistic energy-landscape model. Fits a 1D Ising / correlated potential-energy-landscape (CPEL) model to the JOINT distribution of methylation states in a region (captures CpG-CpG spatial correlation, not marginal per-CpG). Derives MML (mean methylation l…
    stat: Ising MLE; normalized Shannon entropy; JSD for differential testing. COVERAGE: designed for LOW coverage, performs at 5x, robust 7-30x (selling point vs read-counting WSH).
    vs_ISM: informME is the most statistically sophisticated DISORDER tool, but models a per-region entropy/energy SCALAR field, not between-read structure. No read clustering, no PERMANOVA group-separation test, not haplotype-partitioned. ISM tests whether HP/somatic read groups are structurally separated; inf…
    cite: Jenkinson G et al. Nat Genet 2017;49(5):719-729 (PEL, DOI 10.1038/ng.3811); Jenkinson G et al. BMC Bioinformatics 2018;19:87 (informME, DOI 10.1186/s12859-018-2…
- **CpelNano (ONT LONG-READ informME / methylation potential energy landscape) — Abante/Feinberg/Goutsias 2021** [Information-theoretic WSH for OXFORD NANOPORE LONG…] gran=Region-level (default analysis regions up to ~350 bp); model… ✓
    method: DISORDER QUANTITY = region-level methylation entropy/energy under nanopore noise (same NME as informME, on long reads). Extends the informME Ising/CPEL energy-landscape model to ONT via an HMM: true (hidden) methylation state follows an Ising distribution consistent with means + pairwise correlations; observed = nanopo…
    stat: Ising-HMM, EM; NME, MML, CMD; differential landscape testing. COVERAGE: benchmarked at 5x/10x/15x/20x/25x; breast cancer differential analysis partitioned ~271x/249x data into ~25x…
    vs_ISM: THE most directly comparable platform-mate: same ONT long-read substrate as ISM. But CpelNano still produces a region-level entropy/energy SCALAR field (disorder), with NO read-to-read distance matrix, NO clustering into subclonal read groups, NO PERMANOVA structural test, and NO haplotype/somatic-c…
    cite: Abante J, Kambhampati S, Feinberg AP, Goutsias J. Sci Rep 2021;11:21619. DOI 10.1038/s41598-021-00781-x. PMID 34732768
- **CAMDA (Concurrence of Active Methylation and De-methylation) — Shi & Li 2021** [Read-level methylation heterogeneity (concurrence)…] gran=Read-level fragment classification, region-level reported. ✓
    method: DISORDER QUANTITY = within-read co-occurrence of methylated + unmethylated CpGs (a concurrence ratio, distinct from mean and from variance). Dissects each bisulfite read into 3 fragment classes (methylated, unmethylated, 'concurrence' = unmethylated CpGs within partially-methylated reads). Methylation concurrence ratio…
    stat: Weighted fragment ratio; no clustering/permutation structure test. COVERAGE: coverage-dependent (needs enough partially-methylated reads); no explicit minimum stated.
    vs_ISM: Another within-read disorder/concurrence SCALAR (conceptually related to PDR, enzyme-process-framed). No between-read distance geometry, no subclonal clustering, no PERMANOVA, no haplotype/somatic axis. ISM's structural between-molecule test and LOH/CN-coupled somatic cis-test are absent.
    cite: Shi J et al. Nat Commun 2021;12:5285. DOI 10.1038/s41467-021-25521-7. Code: github.com/JiejunShi/CAMDA
- **epiCHAOS — Kelly/Plass 2024** [SINGLE-CELL epigenomic heterogeneity metric; scATA…] gran=Per cluster / cell-group scalar (can be restricted to region… ✓
    method: DISORDER QUANTITY = cell-to-cell epigenetic dissimilarity within a cell group (between-CELL heterogeneity). From a binarized single-cell (peaks/tiles x cells) matrix per cluster, compute pairwise CHANCE-CORRECTED Jaccard distances between all cells (controls for the proportion of 1s/0s), take the mean pairwise distance…
    stat: Chance-corrected Jaccard pairwise distances + mean + linear-regression sparsity correction; chance-centering. COVERAGE: explicitly sparsity/coverage-corrected via regression to avo…
    vs_ISM: Closest in SPIRIT: also uses pairwise (Jaccard) distances + chance correction, parallel to ISM's read x read distance matrix (ISM offers Jaccard among its metrics). BUT epiCHAOS operates BETWEEN SINGLE CELLS (not between reads of one bulk molecule pool), outputs a single per-cluster scalar (no hiera…
    cite: Kelly K et al. Genome Biol 2024;25:296. DOI 10.1186/s13059-024-03446-w. PMID 39623476

## CAT: Methodology survey: quantifying a methylation DIFFERENCE / effect size (delta-beta and its statistical relatives), bench…
- **Beta-value vs M-value (Du et al. 2010)** [transform / metric-definition (array + sequencing-…] gran=per-CpG site-level (probe/locus). Not read-level, not haplot… ✓
    method: Beta = M_intensity/(M+U+alpha), alpha=100 default -> bounded [0,1], 'fraction methylated'. M-value = log2((M+alpha)/(U+alpha)), alpha=1 -> unbounded real line. The two are related by a Logit/logistic function: Beta = 2^M/(2^M+1); M = log2(Beta/(1-Beta)). Verified exact formulas from PMC.
    stat: Compared detection of group differences by a simple t-test on the two representations; M-value shown to have approximately homoscedastic (constant) variance across the methylation …
    hap: No
    vs_ISM: ISM's Δβ is a difference of BETA-values (per-CpG mean methylation per haplotype group). Du 2010 is the canonical warning AGAINST testing on raw beta because of heteroscedasticity at the extremes; it recommends testing on M-values and REPORTING beta. ISM tests Δβ with a non-parametric Wilcoxon signed…
    cite: Du P, Zhang X, Huang CC, Jafari N, Kibbe WA, Hou L, Lin SM. BMC Bioinformatics 2010;11:587. DOI:10.1186/1471-2105-11-587. PMID:21118553. PMC3012676.
- **Raw delta-beta (Δβ) + minimum effect-size threshold** [metric / reporting convention (platform-agnostic)] gran=per-CpG site-level; can be aggregated to region (DMR) by ave… ✓
    method: Δβ = (mean beta group A) - (mean beta group B), per CpG. Reported alongside a significance test. Widely-used practical biological-relevance filter: |Δβ| >= 0.1 (10 percentage points). methylKit's default percent-methylation-difference filter is the same idea at 25% (>25 percentage points).
    stat: No test of its own; Δβ is the EFFECT SIZE that accompanies a p-value from t/Wilcoxon/logistic/beta-binomial. Best-practice literature: report Δβ + CI, do not gate on p alone (stati…
    hap: Only if the two groups are haplotypes (as in ISM HP1 vs HP1-1). Generically it is case-vs-control.
    vs_ISM: ISM's Δβ IS exactly this metric, but (a) groups = haplotypes/alleles not case-control, (b) ISM pairs by CpG and applies Wilcoxon signed-rank (paired non-parametric) rather than an unpaired filter, and (c) adds Cohen d as a standardized companion. The |Δβ|>=0.1 / Cohen ribbon convention is the right …
    cite: Threshold convention is community practice (no single primary theorem). Anchor reporting norm to Xie et al. 2019 (below) + Du 2010. methylKit 25% default: Akali…
- **Xie et al. 2019 - reporting Δβ from M-value models** [methodology / reporting-norm paper (array-oriented…] gran=per-CpG site-level. ✓
    method: Resolves the inconsistency in standard practice where investigators SELECT CpGs and get p-values from an M-value (logit) regression but then report Δβ from a SEPARATE beta-value regression. Shows the latter is biased at extremes; gives 'M-model-coef' and 'M-model-M-mean' methods to back-transform a consistent Δβ from t…
    stat: Linear regression on M-values for inference; derives Δβ as a back-transformed reported effect size. Demonstrates bias when M not in (-2,2) / beta not in (0.2,0.8).
    hap: No
    vs_ISM: Directly relevant to HOW ISM should report Δβ: it warns that test-scale (logit/M, or rank-based) and report-scale (beta) must be made consistent, and that naive beta-scale estimates are biased exactly where ISM operates (highly meth / unmeth CpGs near 0/1). ISM partly sidesteps this by using Wilcoxo…
    cite: Xie C, et al. 'Differential methylation values in differential methylation analysis.' Bioinformatics 2019;35(7):1094-1099. DOI:10.1093/bioinformatics/bty778.
- **BSmooth (Hansen, Langmead, Irizarry 2012; bsseq)** [smoothed regional methylation-difference + signal-…] gran=region-level (DMR) from smoothed per-CpG estimates; SITE-LEV… ✓
    method: Local-likelihood smoothing of the methylation profile: within a window (default 70 CpGs, min 2 kb) fit a 2nd-degree polynomial to logit(f(l)) via a weighted GLM, weights inversely proportional to standard errors, tricube kernel in genomic distance. Produces a smoothed methylation estimate even at low coverage.
    stat: BSmooth.tstat = signal-to-noise t-statistic t(l) = beta_hat(l) / [ sigma_hat(l) * sqrt(1/n1 + 1/n2) ], beta_hat = smoothed group difference, sigma_hat = smoothed across-replicate S…
    hap: No
    vs_ISM: Contrast in philosophy. BSmooth smooths the methylation LEVEL spatially along the genome and tests a per-position SMOOTHED-difference t-statistic across REPLICATES -> it answers 'is the average methylation level shifted in this region'. ISM has no biological replicates (single tumor, two haplotypes …
    cite: Hansen KD, Langmead B, Irizarry RA. Genome Biology 2012;13(10):R83. DOI:10.1186/gb-2012-13-10-r83. PMID:23034175. PMC3491411. Bioconductor pkg: bsseq.
- **DSS - dispersion-shrunken beta-binomial Wald (Feng/Conneely/Wu 2014; Park/Wu 2016)** [per-CpG beta-binomial test on COUNT data (SHORT-RE…] gran=per-CpG DML; DMRs assembled by callDMR (min length, min #CpG… ✓
    method: Models methylated counts as lognormal-beta-binomial hierarchical: M_ijk ~ Binomial(n, p), p ~ Beta(mu, phi); a LOG-NORMAL PRIOR on the dispersion phi enables empirical-Bayes SHRINKAGE of per-CpG dispersion (borrowing strength across sites). Without replicates, neighboring CpGs are treated as pseudo-replicates via movin…
    stat: WALD TEST per CpG on the beta-binomial mean difference, with the variance driven by the shrunken dispersion AND coverage depth. CONFIRMED there is NO arcsine link function in the o…
    hap: No (but it IS the engine NanoMethPhase calls for allele DMA - see below).
    vs_ISM: This is the rigorous count-based answer to the SAME instability problem ISM faces at low haplotype coverage: DSS shrinks per-CpG dispersion and folds coverage into the Wald variance, so a Δβ from 6 reads is down-weighted vs one from 60. ISM currently uses a non-parametric Wilcoxon on per-CpG mean di…
    cite: Feng H, Conneely KN, Wu H. Nucleic Acids Res 2014;42(8):e69. DOI:10.1093/nar/gku154. PMID:24561809. PMC4005660. General design: Park Y, Wu H. Bioinformatics 201…
- **methylKit (Akalin et al. 2012) - logistic regression / Fisher exact** [per-base & per-region differential methylation (SH…] gran=per-base CpG AND per-region (user regions or tiling windows)… ✓
    method: Models per-base methylation proportion P_i with LOGISTIC REGRESSION (methylated vs unmethylated read counts ~ group) when >=1 sample per group with replicates; falls back to FISHER'S EXACT TEST comparing fraction of methylated Cs when there is only ONE sample per group. Reports percent-methylation-difference as the eff…
    stat: Logistic regression null H0: beta1=0; OR Fisher exact (single sample/group). Multiple testing via SLIM (sliding linear model) -> q-values. Default call filter: q<0.01 AND %methylat…
    hap: No
    vs_ISM: methylKit's single-sample-per-group Fisher exact on (meth, unmeth) counts is the closest classical analogue to ISM's per-CpG Fisher exact + BH-FDR (ISM CORE 4, the 'disorder' contrast family). The key divergence: methylKit's effect size is a per-site %methylation difference (a Δβ in percent), tested…
    cite: Akalin A, Kormaksson M, Li S, Garrett-Bakelman FE, Figueroa ME, Melnick A, Mason CE. Genome Biology 2012;13(10):R87. DOI:10.1186/gb-2012-13-10-r87. PMID:2303408…
- **limma moderated-t on M-values (Smyth 2004; eBayes)** [empirical-Bayes linear model on logit/M-values (AR…] gran=per-CpG site-level; designed for arrays, also used on WGBS/E… ✓
    method: Fit a linear model per CpG to M-values; eBayes shrinks the per-CpG residual variances toward a common value (empirical Bayes), producing a MODERATED t-statistic with augmented degrees of freedom. Effect size = the model coefficient on the M scale (back-transform to Δβ per Xie 2019).
    stat: Moderated t = coefficient / shrunken-SE; borrows variance information across CpGs (the variance-shrinkage analogue of what DSS does for dispersion). Requires replicates for the var…
    hap: No
    vs_ISM: limma is the variance-shrinkage gold standard on the CONTINUOUS logit scale, complementary to DSS's dispersion shrinkage on COUNTS. Both solve 'few replicates -> unstable variance' by borrowing strength across CpGs. ISM has no biological replicates, so limma's eBayes denominator (across-sample resid…
    cite: Smyth GK. 'Linear models and empirical Bayes methods for assessing differential expression in microarray experiments.' Stat Appl Genet Mol Biol 2004;3:Article3.…
- **Arcsine sqrt vs logit transform for proportions (Warton & Hui 2011)** [variance-stabilizing transform debate (general sta…] gran=applies to any proportion (per-CpG beta is a proportion). Tr… ✓
    method: Two classical variance-stabilizing transforms for a proportion p: arcsine(sqrt(p)) (asymptotic SD = sigma/(2*sqrt(p(1-p)))) and logit(p)=log(p/(1-p)). Warton & Hui argue arcsine 'is asinine' and that for binomial/proportion data one should prefer logistic (GLM/logit) modeling, checking and accounting for overdispersion…
    stat: Arcsine stabilizes variance to depend only on n (no continuity correction needed) but breaks under extra-binomial overdispersion; logit + GLM has higher power and interpretability.…
    hap: No
    vs_ISM: Provides the first-principles justification for the heteroscedasticity ISM must handle: raw beta differences (Δβ) have mean-dependent variance, smallest near 0 and 1 where most CpGs sit. ISM's choice of a RANK-based Wilcoxon signed-rank (no variance-homogeneity assumption) is a legitimate third opti…
    cite: Warton DI, Hui FKC. 'The arcsine is asinine: the analysis of proportions in ecology.' Ecology 2011;92(1):3-10. DOI:10.1890/10-0340.1. PMID:21560670.
- **Allele-specific methylation difference - CPEL / Ising model (Abante et al. 2020)** [read-level haplotype/allele methylation-pattern di…] gran=READ-LEVEL, multi-CpG joint pattern within haplotype (NOT in… ✓
    method: Models the joint methylation PATTERN across linked CpGs within a haplotype using a 1-D Ising (correlated potential energy landscape, CPEL) model; maximum-likelihood fit by simulated annealing. Compares two homologous alleles via three effect sizes: MML (mean methylation level) imbalance, NME (normalized methylation ent…
    stat: Likelihood-based pattern model + bootstrap empirical null; reliable at low coverage (good at 10-50 reads). Effect sizes are distributional (MML/NME/JSD), not a single Δβ.
    hap: Yes (core design)
    vs_ISM: This is the closest conceptual sibling to ISM and the single most important external comparator. SHARED idea: methylation is a read-level MULTI-CpG PATTERN, not just a per-site mean; both compare two haplotypes' pattern DISTRIBUTIONS. ISM's NME and epipolymorphism (CORE 4) map almost 1:1 onto CPEL's…
    cite: Abante J, Fang Y, Feinberg AP, Goutsias J. 'Detection of haplotype-dependent allele-specific DNA methylation in WGBS data.' Nature Communications 2020;11:5238. …
- **NanoMethPhase - long-read haplotype methylation ASM (Akbari et al. 2021)** [long-read phased methylation + allele DMA (LONG-RE…] gran=region-level ASM (mono-allelic methylated regions) from read… ✓
    method: Phases 5mC nanopore reads to two haplotypes using SNVs (Clair calls, SNVoter filter, min base quality 7, haplotype ratio 0.75), then runs differential methylation analysis between the two haplotypes by DELEGATING TO THE DSS R PACKAGE to call mono-allelic methylated regions.
    stat: Statistics are DSS's beta-binomial dispersion-shrunken Wald (it does not invent its own test). Works at ~10x coverage.
    hap: Yes
    vs_ISM: The most direct LONG-READ comparator and the right citation for 'what other people do with ONT haplotype methylation'. SAME platform (ONT long-read), SAME phasing-then-compare-haplotypes skeleton as ISM. CRITICAL contrasts for the paper: (1) NanoMethPhase outsources its difference test to DSS (per-C…
    cite: Akbari V, Garant JM, O'Neill K, et al. 'Megabase-scale methylation phasing using nanopore long reads and NanoMethPhase.' Genome Biology 2021;22:68. DOI:10.1186/…
- **Cohen's d (paired d_z) effect-size reporting norm** [standardized effect-size convention (general stati…] gran=summary statistic over the paired per-CpG Δβ set; not spatia… ✓
    method: Standardized mean difference = mean difference / SD. Paired form d_z = mean(paired differences) / SD(differences) - exactly what pairs with a Wilcoxon signed-rank / paired test. Benchmarks: d=0.2 small, 0.5 medium, 0.8 large.
    stat: Unit-free; complements (does not replace) the p-value. For rank tests an alternative is r = Z/sqrt(n).
    hap: n/a
    vs_ISM: ISM reports Cohen d alongside mean Δβ and Wilcoxon signed-rank - this matches best practice (report magnitude + precision, not just p). Since ISM pairs by CpG, the correct flavor is the PAIRED d_z (mean of CpG-wise differences / SD of those differences), and the paper should say so explicitly to avo…
    cite: Cohen J. 'Statistical Power Analysis for the Behavioral Sciences' (2nd ed, 1988). Paired d_z: Lakens D. Front Psychol 2013;4:863. DOI:10.3389/fpsyg.2013.00863.

[total survey tools = 82]


# DEEP-READ DIGEST


## DEEP: ONT modkit v0.4.x — `modkit dmr` (subcommands `pair` / `multi`) and `modkit entropy`. Long-read (ONT mod-BAM, MM/ML tags), reference-anchored. Designed primarily for germline/general 5mC|5hmC vs C differential methylation and epiallele randomness; NOT a cancer-specific or tumor-normal-paired tool, NOT short-read/array. Region-level (DMR) or within-read/within-window (entropy); haplotype handling is done UPSTREAM in `modkit pileup`, not inside `dmr`/`entropy`.  (src: PRIMARY SOURCES FETCHED & VERIFIED (all official nanoporetech.github.io / raw GitHub book source): (1) https://nanoporet…)
  method_detail: === modkit dmr — EXACT method (all verbatim-confirmed from primary docs) ===  INPUT: two or more bedMethyl files produced by `modkit pileup` (must be bgzip-compressed + tabix-indexed). dmr operates on AGGREGATED per-genomic-position counts (methylated count / valid coverage), NOT on individual reads. Subcommands: `pair` (two conditions A vs B; single sites if `--regions/-r` omitted, or regions if BED given) and `multi` (all pairwise comparisons across ≥3 samples; regions only; needs `-o` output dir + per-sample names).  CORE STATISTIC (verbatim): score = log( l_{θa}(X_a) · l_{θb}(X_b) / l_{θ_{a+b}}(X_{a+b}) ). This is a LOG MARGINAL LIKELIHOO…
  stat_detail: dmr STATISTICS: (a) primary = log marginal likelihood ratio score = log[ l_{θa}(X_a)·l_{θb}(X_b) / l_{θ_{a+b}}(X_{a+b}) ], a Bayes-factor-style comparison of separate-vs-shared methylation parameter models; marginal likelihoods integrate the parameter out under the conjugate prior (so NOT a plug-in MLE GLRT). (b) likelihood model = Beta-Binomial (b…
  vs_ISM_detailed: POINT-BY-POINT vs ISM cores 1-6 + Δβ (ISM facts from source-verified InterSubMod/docs/method_comparison/20260609_ism_vs_external_methylation_tools/01_ism_method_spec_from_source.md):  AXIS — what is measured: - modkit dmr: BETWEEN-SAMPLE/BETWEEN-CONDITION difference in the per-position METHYLATION RATE (β). It is a rate-difference engine on aggregated pileup counts. - modkit entropy: WITHIN-MOLECULE RANDOMNESS of epiallele patterns (disorder/PDR-family). - ISM cores 1-3: BETWEEN-MOLECULE STRUCTURE — does the read↔read distance space separate by HP/somatic labels. ISM explicitly measures who-clusters-with-whom, not rate and not within-read disorder. → dmr ≈ ISM's β-RATE family (core 6's Δβ + core 4a Fisher per-CpG); entropy ≈ ISM's core 4b N…
  ISM_could_learn: Concrete adoptions ISM could make from modkit: (1) Replace binarized per-CpG Fisher (core 4a) with the Beta-Binomial marginal-likelihood-ratio + Jeffreys-prior shrinkage for the per-CpG/Δβ difference test — gives better small-count behaviour and a coherent Bayes-factor magnitude alongside the existing Cohen's d. (2) Adopt Cohen's h (variance-stabilized arcsine) as a high-count-robust companion to ISM's Δβ Cohen's d, since targeted cancer loci often hit saturating coverage where raw β-difference …
  limitations: modkit dmr/entropy limitations for OUR context (ONT long-read CANCER somatic ASM): (1) dmr operates on AGGREGATED per-position pileup counts — it cannot see read-level co-structure (no read×read distance, no subclonal clustering). It answers 'is the …

## DEEP: pycoMeth v2.2.2 (Snajder, Leger, Stegle, Bonder 2023, Genome Biology 24:83). A Python toolbox for differential methylation testing on ONT long-read methylation calls, downstream of Nanopolish call-methylation. LONG-READ, READ-LEVEL storage (MetH5 HDF5), but the differential test and the segmentation both REDUCE to region/interval-level units. GERMLINE-oriented by design (benchmarks: GIAB trio chr20 parent-of-origin / imprinting; tumor-vs-normal is supported via read-groups but not the validation focus). Two modules: Meth_Comp (differential test) + Meth_Seg (Bayesian HMM consensus segmentation). Verified against the PMC full text AND the actual source on github.com/PMBio/pycoMeth@main (Meth_Comp.py, meth_seg/hmm.py, meth_seg/emissions.py).  (src: VERIFIED: (1) PMC full text PMC10120131 (https://pmc.ncbi.nlm.nih.gov/articles/PMC10120131/) -- algorithm + benchmark nu…)
  method_detail: VERIFIED FROM SOURCE (Meth_Comp.py, hmm.py, emissions.py on main) + PMC10120131.  == Meth_Comp differential test == Input is an aggregated interval (from CpG_Aggregate / Interval_Aggregate / Meth_Seg segments), NOT a raw read pile. Three hypotheses, each picking a scipy test via pvalue_method: 1. llr_diff: pools every per-read CpG log-likelihood-ratio (LLR) across reads in the interval into one vector per sample; two-sided Mann-Whitney-U (MW, 2 samples) or Kruskal-Wallis (KW, >2). This is the only mode that uses raw per-call LLR (so it 'draws power from segment size + read depth + call uncertainty'). It is read-level-derived but operates on a…
  stat_detail: VERIFIED scipy/statsmodels calls in Meth_Comp.py (line 15 import; lines ~189-251): from scipy.stats import kruskal, mannwhitneyu, wilcoxon, fisher_exact, chi2_contingency; from statsmodels.stats.multitest import multipletests. pvalue_method enum: 'MW' -> mannwhitneyu(...,alternative='two-sided'); 'KW' -> kruskal(*test_values) + post-hoc one-vs-othe…
  vs_ISM_detailed: POINT-BY-POINT vs ISM (InterSubMod), grounded in our project memory (5mCG read-level subclonal, read x read distance matrix, normal-anchored cis-test, MSA Level-1 5mC/5hmC, ARI/PERMANOVA HP-group structure).  1) UNIT OF ANALYSIS / read x read distance matrix: - pycoMeth has NO read x read distance matrix. Even its most read-aware mode (bs_diff) collapses each read to a single scalar beta-score, then compares two/three SCALAR DISTRIBUTIONS with MW/KW. llr_diff pools all CpG LLRs into one vector. count_dependency is a 2x2 count table. None preserves the read x read or read x CpG matrix. - ISM keeps the full read x CpG methylation matrix and builds a read-read distance matrix (distance_matrix_[METRIC].csv per region), then tests structure with…
  ISM_could_learn: (1) ADOPT THE SOFT-CALL EMISSION: ISM Level-1 thresholds CpGs to binary; pycoMeth's BernoulliPosterior keeps p(methylated) from the LLR/ML probability. ISM's read x CpG matrix and distance metrics could be built on soft probabilities (expected methylation) instead of hard 0/1, propagating ML uncertainty -- likely improves the read-read distance matrix and reduces low-coverage noise. (2) JOINT MULTI-SAMPLE/HAPLOTYPE SEGMENTATION: pycoMeth's shared-changepoint, per-group-mu consensus HMM is a temp…
  limitations: (a) GERMLINE-CENTRIC: validated on GIAB trio parent-of-origin / imprinting; NO somatic model, NO tumor-vs-normal causal control, NO normal-anchoring -> cannot separate somatic ASM from baseline germline allelic methylation (the exact confound our pro…
  verified_claims (8):
    - Meth_Comp llr_diff = two-sided Mann-Whitney-U (2 samples) / Kruskal-Wallis (>2) on pooled per-read CpG log-likelihood ratios; bs_diff runs the same MW/KW (or pa…  [github.com/PMBio/pycoMeth/blob/main/pycoMeth/Meth_Comp.py (scipy import line 15; pvalue_me…]
    - Multiple-testing correction default = Benjamini-Hochberg (statsmodels multipletests 'fdr_bh'), with optional IHW weighting where weight = standard deviation of …  [github.com/PMBio/pycoMeth/blob/main/pycoMeth/Meth_Comp.py (multipletests import; compute_i…]
    - Meth_Seg is a Bayesian changepoint detector modeled as a left-to-right HMM; emission = Bernoulli likelihood with uncertain (soft) observations p(methylated) fro…  [github.com/PMBio/pycoMeth/blob/main/pycoMeth/meth_seg/hmm.py (SegmentationHMM, baum_welch,…]
    - Haplotype-aware = haplotypes/samples stored as read-groups in MetH5; the HMM fits ONE shared consensus segmentation but SEPARATE methylation rate mu per read-gr…  [PMC10120131 + pmbio.github.io/pycoMeth/Meth_Seg.html + emissions.py mu_{s,g} per cluster/s…]
    - MetH5 = read-level, reference-anchored HDF5 store of per-CpG LLRs with read-group annotations; 5-90x faster random access than BAM/CRAM  [PMC10120131 + github.com/PMBio/pycoMeth README]
    - Meth_Seg defaults WINDOW_SIZE=300 CpG sites, MAX_SEGMENTS_PER_WINDOW=10; Meth_Comp defaults MIN_NUM_READS_PER_INTERVAL=10, MAX_MISSING=0  [pmbio.github.io/pycoMeth/Meth_Seg.html + Meth_Comp.html]

## DEEP: cvlr (Raineri, Alberola i Pla, Dabad, Heath — CNAG-CRG / CRG Barcelona). "cvlr: finding heterogeneously methylated genomic regions using ONT reads." Bioinformatics Advances 3(1):vbac101, published 2023-01-23. DOI 10.1093/bioadv/vbac101; PMID 36726731. A command-line tool that clusters individual ONT long reads by their methylation pattern using a mixture of multivariate Bernoulli distributions fit by EM, WITHOUT a prior phasing step. Three executables: cvlr-cluster, cvlr-meth-of-bam, cvlr-stats. SCOPE TAGS: long-read ONT-specific (tested on NA12878 lymphoblastoid line, Nanopore Consortium); GERMLINE-demonstrated (imprinting / allele-specific benchmark) — cancer/somatic NOT demonstrated; operates READ-level (clusters reads) but reports aggregated to genomic windows/regions (5 kb genome scans). NOTE: the GitHub repo cited in the paper (github.com/EmanueleRaineri/cvlr) is NOT publicly reachable as of 2026-06-10 (404; author account shows public_repos:0) — implementation language and exact CLI flags could NOT be verified from primary source and are marked UNVERIFIED.  (src: VERIFIED-FETCHED: (1) PMC full text https://pmc.ncbi.nlm.nih.gov/articles/PMC9887406/ — algorithm, covariance eq, M-step…)
  method_detail: ALGORITHM (verified from PMC9887406 full text + Oxford Academic article page). Data: binary matrix X with n reads (rows) x d genomic CpG positions (columns); each X_ij in {0,1,-1}, where -1 = missing methylation call (read does not cover that CpG, or call below confidence). MODEL: mixture of k multivariate Bernoulli distributions. Parameters: (a) mixing proportions pi_l, l=1..k, with sum pi_l = 1; (b) a k x d matrix mu, where row mu_l gives the per-cluster per-CpG methylation probability ('average methylation for each cluster at each position'). Per-read likelihood is the standard mixture: sum_l pi_l * product_j [ mu_lj^x_ij (1-mu_lj)^(1-x_ij…
  stat_detail: k-cluster mixture of multivariate Bernoulli fit by EM. Parameters: pi (k mixing weights, sum=1) and mu (k x d per-cluster per-CpG methylation probabilities). E-step = posterior cluster responsibilities per read; M-step = update pi, mu with missing (-1) entries imputed by weighted current cluster means. Random init + multiple seeds, keep best likeli…
  vs_ISM_detailed: DIRECT COMPARISON — both cluster individual long reads by methylation pattern with NO prior phasing, on ONT data; the difference is the clustering ENGINE and the statistical treatment of uncertainty. (1) CLUSTERING PARADIGM. cvlr = generative model-based: a finite mixture of multivariate Bernoulli distributions fit by EM; every read gets a SOFT posterior responsibility (probability of cluster membership) and the model produces interpretable per-cluster per-CpG methylation probabilities mu_l. ISM = distance-based agglomerative: it first builds a read x read distance matrix (verified from source include/core/Types.hpp enum {NHD, L1, L2, CORR, JACCARD, BERNOULLI}; Config.hpp default = BERNOULLI distance; CLI --distance-metric default NHD) then…
  ISM_could_learn: Three concrete, defensible borrowings — with the caveat that ISM already covers part of what the prompt assumed it lacked. (A) SOFT PROBABILISTIC MEMBERSHIP (highest value). cvlr's EM gives each read a posterior probability of cluster membership; this directly surfaces 'ambiguous' reads near a subclone boundary, which in a cancer somatic-methylation context is exactly where read-misassignment corrupts downstream TP/FP / subclonal calls. ISM's hard UPGMA cut throws this away. Adding a model-based…
  limitations: No data-driven selection of k — user must fix the number of clusters (examples use k=2); no BIC/AIC/likelihood-ratio criterion reported. No reported ARI, sensitivity/specificity, or runtime. Bernoulli mixture assumes within-cluster CpG independence (…
  verified_claims (12):
    - cvlr clusters ONT reads into k clusters via a mixture of multivariate Bernoulli distributions fit by an Expectation-Maximization (EM) algorithm.  [https://academic.oup.com/bioinformaticsadvances/article/3/1/vbac101/6998217 (verbatim: 'Re…]
    - The method does NOT rely on a prior phasing step, letting it capture allele-specific AND non-allelic (e.g. cell-subpopulation) heterogeneity.  [https://academic.oup.com/bioinformaticsadvances/article/3/1/vbac101/6998217 (verbatim: 'Si…]
    - The number of clusters k is user-specified; no automatic BIC/AIC model selection is reported (examples and the chr15 scan use k=2).  [https://pmc.ncbi.nlm.nih.gov/articles/PMC9887406/ and Oxford Academic article ('allow[s] t…]
    - Missing methylation calls are encoded as -1 in the read x position matrix and the M-step imputes them with a weighted sum of the corresponding positions in the …  [https://pmc.ncbi.nlm.nih.gov/articles/PMC9887406/ (verbatim: 'The M step updates the mu ma…]
    - Covariance is sum_l pi_l{Sigma_l + mu_l mu_l^T} - E[x]E[x]^T with Sigma_l = diag(mu_l(1-mu_l)).  [https://pmc.ncbi.nlm.nih.gov/articles/PMC9887406/]
    - On chr15, WhatsHap left ~40% of reads (130,963 of 329,353) unassigned to a haplotype because they lack informative SNPs, whereas cvlr produced results on the 5 …  [https://pmc.ncbi.nlm.nih.gov/articles/PMC9887406/]

## DEEP: DAMEfinder — Orjuela, Machlab, Menigatti, Marra & Robinson. Journal = Epigenetics & Chromatin (NOT NAR Genomics & Bioinformatics as the task prompt stated — corrected from primary source). 2020, vol 13, article 25. DOI 10.1186/s13072-020-00346-8; PMID 32487212; PMCID PMC7268773. Bioconductor R/BioC package (markrobinsonuzh/DAMEfinder). NOTE: the ISM C++ source cites it as "De Waele 2020" in PerCpgAsm.cpp:6 / PerCpgAsm.hpp:8 — that author attribution is ALSO wrong; the DAMEfinder authors are Orjuela et al. (De Waele 2020 is a different methylation paper). Both the task prompt's journal and the ISM source's author tag are mis-cited.  (src: PRIMARY (fetched & verified): (1) DAMEfinder paper full text — PMC7268773 / Epigenetics & Chromatin 2020 13:25, DOI 10.1…)
  method_detail: DAMEfinder is a short-read bisulfite-sequencing (WGBS / targeted BS-seq; bismark + Bowtie2 alignment) tool with TWO ASM modes that converge at the differential-testing stage.  (A) SNP-based / "exhaustive" mode (needs a VCF of heterozygous SNPs; pipeline extract_bams() -> calc_derivedasm()). For each CpG covered by reads spanning a het SNP, reads are split by allele and the ASMsnp score (Eq.1) is the difference in methylated-read fraction between ref and alt alleles: ASMsnp_i = (XM_ir / X_ir) - (XM_ia / X_ia), range 0 (equal) to 1 (full asymmetry). XM = methylated reads on that allele, X = total reads on that allele.  (B) Tuple-based / "fast" …
  stat_detail: DAMEfinder: ASMsnp = ref/alt methylated-fraction difference (Eq.1, 0-1). ASMtuple = weighted log10 odds-ratio (MM*UU)/(MU*UM) with Beta(gamma+XMM, gamma+XUU) weight (Eq.2-4). Differential test = limma moderated t (lmFit + eBayes empirical-Bayes variance shrinkage) on sqrt-transformed scores, multi-sample design matrix; region calling = Simes cluste…
  vs_ISM_detailed: UNIT OF TEST: DAMEfinder tuple mode scores CpG-PAIRS (read concordance across two CpGs on the same read = on-read epigenetic linkage); ISM scores SINGLE CpGs independently (a 2x2 per site). DAMEfinder's whole premise is the on-read MM/UU vs MU/UM pattern; ISM's per-CpG Fisher throws away the cross-CpG read pattern at the Fisher stage (it captures on-read structure separately, in NME/epipolymorphism). So ISM's "per-CpG Fisher" is genuinely analogous to a per-site DM test, NOT to DAMEfinder's signature tuple log-odds.  GROUPING / ALLELE AXIS: DAMEfinder splits reads by ALLELE — either explicit SNP allele (ref vs alt, SNP-based mode) or implicit bimodality (tuple mode infers allele-specificity from MM/UU split without phasing). ISM splits read…
  ISM_could_learn: RANKED, honest about whether each fits ONT long-read somatic single-tumor context:  1. ADOPT THE TUPLE (CpG-PAIR) LOG-ODDS as a complement to per-CpG Fisher — HIGH FIT, HIGH VALUE. DAMEfinder's core insight is that on-read MM/UU vs MU/UM concordance carries allele-specificity that single-CpG tests miss. ISM has the IDEAL substrate for this (full read x CpG methylation matrix already in MethylationMatrix; long reads span many CpGs >> 150 bp cap, so ISM can compute tuples at arbitrary distance wit…
  limitations: DAMEfinder limits for THIS project: short-read BS-seq only (no ONT/long-read), 150bp tuple-distance cap (short-read paired-end artifact), SNP mode re-derives alleles per-SNP via Bis-SNP (inferior to ISM's existing phased long-read HP tags), designed …
  verified_claims (7):
    - DAMEfinder published in Epigenetics & Chromatin 2020, vol 13 article 25, DOI 10.1186/s13072-020-00346-8, PMID 32487212, PMCID PMC7268773; authors Orjuela, Machl…  [https://pmc.ncbi.nlm.nih.gov/articles/PMC7268773/]
    - Tuple-based ASM score = log-odds ratio of (MM*UU)/(MU*UM) over CpG pairs, multiplied by a Beta-distribution weight w_i (Eq.2-4), defaults gamma=0.5, eps=0.2, ma…  [https://pmc.ncbi.nlm.nih.gov/articles/PMC7268773/]
    - SNP-based ASMsnp score (Eq.1) = (XM_ir/X_ir) - (XM_ia/X_ia): difference in methylated-read fraction between ref and alt alleles, range 0 to 1; SNPs called with …  [https://pmc.ncbi.nlm.nih.gov/articles/PMC7268773/]
    - Differential ASM (find_dames) uses limma lmFit + eBayes moderated t-statistic per CpG on sqrt-transformed ASMtuple scores, then region p-values via Simes (defau…  [https://www.bioconductor.org/packages/release/bioc/vignettes/DAMEfinder/inst/doc/DAMEfinde…]
    - Two modes: SNP-based ('exhaustive', needs VCF het SNPs, extract_bams->calc_derivedasm) and tuple-based ('fast', SNP-free, methtuple->read_tuples->calc_asm). Tup…  [https://www.bioconductor.org/packages/release/bioc/vignettes/DAMEfinder/inst/doc/DAMEfinde…]
    - DAMEfinder is short-read bisulfite-sequencing only (bismark + Bowtie2 / methtuple / Bis-SNP); README and paper give NO long-read / Nanopore / ONT support.  [https://github.com/markrobinsonuzh/DAMEfinder]

## DEEP: DSS (Dispersion Shrinkage for Sequencing data) — Bioconductor R package, v2.60.0, maintained by Hao Wu (Emory) & Hao Feng (Case Western). Gold-standard SHORT-READ bisulfite-sequencing (WGBS/RRBS) DML/DMR statistic. Three foundational methodology papers verified from primary sources: (1) Feng, Conneely & Wu 2014, Nucleic Acids Res 42(8):e69, DOI 10.1093/nar/gku154, PMID 24561809 — beta-binomial + lognormal dispersion shrinkage + per-CpG Wald (DML, two-group); (2) Wu, Xu, Feng, Chen, Li, Yao, Qin, Jin & Conneely 2015, Nucleic Acids Res 43(21):e141, DOI 10.1093/nar/gkv715, PMID 26184873 — smoothing + empirical-Bayes dispersion WITHOUT replicates (DMR); (3) Park & Wu 2016, Bioinformatics 32(10):1446-1453, DOI 10.1093/bioinformatics/btw026 — beta-binomial regression with arcsine link for general experimental design (DMLtest.multiFactor). Package biocViews: DNAMethylation, DifferentialMethylation; covers DE genes (RNA-seq) + DML/DMR (BS-seq). PLATFORM CONTEXT (critical): built/validated for SHORT-READ count-based bisulfite data; NO long-read/nanopore (ONT MM/ML) support. Compares GROUPS OF SAMPLES (germline/case-control) at REGION level, not read-groups/haplotypes within one sample; not designed for single-sample somatic clonal/allele-specific structure. So for our context (ONT long-read, somatic cancer, read-level/haplotype Delta-beta) DSS is a STATISTICS BLUEPRINT and citation/benchmark, NOT a drop-in tool.  (src: )
  method_detail: DSS pipeline = 4 statistical stages, all verified:  1) BETA-BINOMIAL COUNT MODEL (2014; 2016). Methylated read count Y_id ~ beta-binomial(m_id, pi_id, phi_i): m_id=coverage, pi_id=methylation proportion, phi_i=CpG-site-specific dispersion (over-dispersion beyond binomial). Captures BOTH technical (binomial sampling) AND biological (between-replicate) variation; a plain binomial/Fisher assumes phi=0 and underestimates variance.  2) BAYESIAN/EMPIRICAL-BAYES DISPERSION SHRINKAGE (the namesake; 2014). Per-site phi_i is poorly estimated with few replicates, so DSS imposes a LOG-NORMAL prior on dispersions (2014 Fig 1: dispersion is approximately l…
  vs_ISM_detailed: ISM uses Delta-beta (difference in mean methylation fraction between two read groups) + per-CpG Fisher exact, with NO dispersion shrinkage, NO smoothing. Head-to-head:  A) VARIANCE MODEL (biggest gap). Fisher on a 2x2 (methylated/unmethylated x group) assumes each read is an independent Bernoulli draw = binomial with phi=0. DSS's beta-binomial adds over-dispersion phi_i. In ONT long-read SOMATIC data, reads from the same haplotype/clone are NOT independent (epipolymorphism, clonal structure, allele-specific methylation) -> true biological over-dispersion >0 -> Fisher UNDERESTIMATES variance -> p-values too small -> inflated significant-CpG calls.  B) DISPERSION SHRINKAGE. DSS stabilizes per-site dispersion by borrowing across sites (and nei…
  ISM_could_learn: Prioritized transfers to make ISM Delta-beta effect sizes/CIs robust (ranked by ROI for ONT single-sample somatic read-level work):  1) [HIGHEST] REPLACE Fisher with a per-CpG BETA-BINOMIAL model. Fit Y ~ beta-binomial(coverage, pi_group, phi) so over-dispersion among reads of the same group is explicitly absorbed. Directly fixes the false-positive inflation from treating non-independent long reads as independent Bernoulli draws. Note: prior O11 epipolymorphism NEGATIVE was 'all n_reads confound…
  verified_claims (7):
    - DSS uses a beta-binomial model: Y_id ~ beta-binomial(m_id, pi_id, phi_i) with phi_i a CpG-site-specific dispersion capturing biological + technical variation.  [Park & Wu 2016, Bioinformatics 32(10):1446-1453, DOI 10.1093/bioinformatics/btw026 (verifi…]
    - Dispersion is modeled with a LOG-NORMAL prior and shrunk via a lognormal-beta-binomial Bayesian hierarchical model that borrows information across CpG sites; Fi…  [Feng, Conneely & Wu 2014, Nucleic Acids Res 42(8):e69, DOI 10.1093/nar/gku154, PMID 245618…]
    - DML detection uses a per-CpG Wald test (statistic ~ N(0,1)) after plugging in shrunk dispersion.  [Feng/Conneely/Wu 2014 (PMID 24561809) and Park & Wu 2016 (DOI 10.1093/bioinformatics/btw02…]
    - The 2016 general-design model uses a beta-binomial REGRESSION with an ARCSINE link arcsin(2*pi_id-1)=x_d*beta_i; arcsine stabilizes variance enabling GLS estima…  [Park & Wu 2016, Bioinformatics 32(10):1446-1453, DOI 10.1093/bioinformatics/btw026 (verifi…]
    - DSS can detect DMRs WITHOUT replicates via local moving-average smoothing (500bp default) + empirical-Bayes dispersion using nearby CpGs as pseudo-replicates; W…  [Wu, Xu, Feng, Chen, Li, Yao, Qin, Jin & Conneely 2015, Nucleic Acids Res 43(21):e141, DOI …]
    - callDMR merges significant per-CpG calls into regions using p.threshold, delta, minlen, minCG, dis.merge, pct.sig; DMRs ranked by areaStat = sum of per-CpG test…  [The DSS User's Guide (official Bioconductor vignette), bioconductor.org/packages/devel/bio…]

## DEEP: Guo et al. 2017, "Identification of methylation haplotype blocks aids in deconvolution of heterogeneous tissue samples and tumor tissue-of-origin mapping from plasma DNA." Nature Genetics 49(4):635-642. DOI 10.1038/ng.3805, PMID 28263317. Companion software: MONOD2 (github.com/dinhdiep/MONOD2). SHORT-READ bisulfite sequencing (WGBS / RRBS / 450K array). GERMLINE/cell-mixture deconvolution + cancer cfDNA tissue-of-origin (NOT somatic-variant-level, NOT long-read). Region/locus level summary statistic computed FROM read-level co-methylation.  (src: PRIMARY/CONFIRMED: PubMed PMID 28263317 (full bib + abstract: 147,888 blocks, r2 LD between adjacent CpGs, MHL, 59-patie…)
  method_detail: VERIFIED ALGORITHM (primary sources below):  (1) MHB DEFINITION — "linkage disequilibrium of methylation" between ADJACENT CpG sites on the SAME read. A read covering CpG_a and CpG_b gives a 2x2 contingency of methylated/unmethylated states across reads; r2 is the standard population-genetics LD r2 applied to binary methylation states (treating M/U like two alleles). MONOD2 README (official): "an algorithm greedily selects the largest possible continuous region with a minimum linkage disequilibrium score" → blocks grow as long as every adjacent CpG pair stays above the r2 cutoff; a pair below cutoff breaks the block. THRESHOLDS (verified acro…
  stat_detail: LD r2 here = the standard squared correlation of two binary states applied to methylation. For two CpG positions covered by the same set of reads, build allele-like frequencies p_A=fraction methylated at site A, p_B at site B, p_AB=fraction of reads methylated at BOTH; D = p_AB - p_A*p_B; r2 = D^2 / [p_A(1-p_A) p_B(1-p_B)]. Thresholds: seed >=2 adj…
  vs_ISM_detailed: PRECISE AXIS DISTINCTION — this is the load-bearing answer:  MHB/MHL operates on the WITHIN-READ, ALONG-THE-GENOME axis (intra-molecular, CpG-vs-CpG). The unit of linkage is a PAIR OF CpG POSITIONS measured down a single read; r2 asks "do CpG_a and CpG_b tend to share the same methylation state within the same molecule?" The output is a per-LOCUS/per-BLOCK scalar (MHL) summarizing how tightly co-methylated neighboring CpGs are. Reads are the OBSERVATIONS used to estimate pairwise CpG coupling; the reads themselves are never compared to each other. Axis = "horizontal" along genomic coordinate, collapsed over reads.  ISM read-read distance operates on the BETWEEN-READ axis (inter-molecular). The unit is a PAIR OF READS; the metric is a distan…
  ISM_could_learn: (1) The MHB block-finder is directly adoptable: ISM could replace fixed regions with LD-r2 greedy co-methylation blocks to define analysis windows in a data-driven way (co-methylated functional units), then run read-read distance WITHIN each block — combining MHB's column-axis block definition with ISM's row-axis read partitioning. (2) MHL is a cheap, coverage-robust per-region scalar that could serve as a FIRST-PASS screen: regions with high MHL coordination are candidates where read-read struc…
  limitations: (1) EXACT w_i NOT VERBATIM-VERIFIED: the precise analytic weight (is w_i=i exactly, or another increasing function?) and the exact substring-cap rule appear only in the paywalled Online Methods/supplement of ng.3805; secondary sources confirm structu…
  verified_claims (8):
    - Guo, Diep, Plongthongkum, Fung, Zhang K, Zhang K. Nature Genetics 2017, 49(4):635-642, DOI 10.1038/ng.3805, PMID 28263317.  [https://pubmed.ncbi.nlm.nih.gov/28263317/]
    - 147,888 methylation haplotype blocks identified across the genome; built from 61 WGBS datasets, validated with 101 RRBS + 637 methylation-array datasets (short-…  [https://pubmed.ncbi.nlm.nih.gov/28263317/]
    - MHBs defined by methylation linkage disequilibrium between adjacent CpG sites; stringent coupling illustrated at r2 > 0.9.  [https://pubmed.ncbi.nlm.nih.gov/28263317/ (PubMed/Nature abstract); r2>0.9 figure-level de…]
    - Operational MHB seed = a minimum of 2 adjacent CpG sites with linkage disequilibrium r2 > 0.5 (restated verbatim from the identical Guo 2017 method).  [https://pmc.ncbi.nlm.nih.gov/articles/PMC7993581/]
    - MONOD2 (Guo 2017 official toolkit): block-finder 'greedily selects the largest possible continuous region with a minimum linkage disequilibrium score'.  [https://github.com/dinhdiep/MONOD2]
    - MHL = weighted mean of the fraction of fully methylated/unmethylated haplotypes and their substrings at different lengths; for a block of length L (>=10) substr…  [https://pmc.ncbi.nlm.nih.gov/articles/PMC7993581/ and https://academic.oup.com/bioinformat…]

## DEEP: Metheor — Lee, Lee & Yu, "Metheor: Ultrafast DNA methylation heterogeneity calculation from bisulfite read alignments", PLOS Computational Biology 2023 Mar 20;19(3):e1010946. DOI: 10.1371/journal.pcbi.1010946. PMID/PMCID: PMC10062925. Source code: https://github.com/dohlee/metheor (GPL-3.0, Rust, distributed via conda). bioRxiv preprint: 10.1101/2022.07.20.500893. NOTE on provenance: this tool is short-read BISULFITE sequencing (Bismark-aligned, XM tags), GERMLINE/cell-line heterogeneity context (CCLE, Ewing sarcoma RRBS) — NOT ONT long-read, NOT somatic-caller-aware, NOT haplotype-aware. Every claim below is verified against the primary source noted; bioRxiv HTML+PDF returned HTTP 403 so a handful of exact LaTeX equations could not be lifted verbatim and are reconstructed from PMC + the canonical WSHPackage/Scherer reference and marked accordingly.  (src: VERIFIED & FETCHED: (1) Metheor paper PMC full text https://pmc.ncbi.nlm.nih.gov/articles/PMC10062925/ — metrics list, L…)
  method_detail: CORE ALGORITHM — "one-sweep" / read-centric. Metheor's central efficiency claim (VERIFIED, PMC10062925): it is a read-centric algorithm that iterates over the aligned reads exactly ONCE, accumulating per-locus heterogeneity tallies as it goes. This contrasts with the CpG-centric design of the reference R implementation (WSHPackage), which for every CpG re-fetches all reads covering that CpG via the indexed BAM. Stated complexity: Metheor needs ~n read accesses (n = number of aligned reads); CpG-centric tools need ~λ·n accesses where λ = average number of CpGs per read. Because RRBS/bisulfite preferentially targets CpG-dense regions, λ>1, so t…
  stat_detail: LPMD ALGORITHM, precise (VERIFIED GitHub docs + PMC): - Iterate each read once. On a read, enumerate all CpG pairs (i,j) whose genomic separation d = pos_j - pos_i satisfies min_distance <= d <= max_distance (defaults 2bp..16bp). - For each such pair, both CpGs have a binary state (M/U from the Bismark XM string). The pair is CONCORDANT if states e…
  vs_ISM_detailed: THE AXIS DISTINCTION (the user's central question) — CONFIRMED and sharpened.  Metheor distance = WITHIN-READ, BETWEEN-POSITION (CpG_i vs CpG_j on the same molecule, j-i = fixed genomic bp gap). LPMD's pair is "two CpGs at distance d on the same read"; discordance = the two CpGs have different methylation states. The genomic-distance axis is the x-axis. It answers: "at this locus, how locally disordered is the methylation comb WITHIN single molecules?"  ISM distance = BETWEEN-MOLECULE, READ-READ (read_a vs read_b across the SAME set of CpG positions). ISM's distance_matrix_[METRIC].csv (per output-structure.md) is Read × Read; the methylation.csv is Read × CpG and the distance collapses the CpG axis to compare whole molecules to each other.…
  ISM_could_learn: CONCRETE, RANKED BY ROI: (1) BORROW THE WITHIN-READ AXIS AS A COMPLEMENTARY PER-LOCUS FEATURE (highest value). Add an LPMD-style scalar — fraction of discordant within-read CpG pairs at distance d, swept over several d bins (on long reads you can go well past 16bp: e.g. 2-16, 16-100, 100-500bp) — computed during ISM's existing single-pass read sweep. This gives ISM a within-read disorder feature that is mathematically orthogonal to its read-read clustering, directly addressing "what does intra-m…
  limitations: (1) Could not fetch bioRxiv verbatim equations (403) — LPMD/ME formulas are reconstructed from PMC prose + Scherer canonical defs, marked UNVERIFIED at the LaTeX level (the ratio form and entropy/epipolymorphism forms are high-confidence; any exact n…
  verified_claims (8):
    - Metheor published PLOS Comput Biol 2023 Mar 20;19(3):e1010946, DOI 10.1371/journal.pcbi.1010946, Rust, GPL-3.0, conda-distributed  [https://pmc.ncbi.nlm.nih.gov/articles/PMC10062925/ + https://github.com/dohlee/metheor]
    - Metheor computes 6 reimplemented heterogeneity metrics (PDR, FDRP, qFDRP, MHL, Epipolymorphism/PM, Methylation Entropy/ME) and proposes LPMD as novel  [https://pmc.ncbi.nlm.nih.gov/articles/PMC10062925/]
    - LPMD = n_discordant / (n_concordant + n_discordant) over CpG pairs at fixed genomic distance on the same read; defaults --min-distance 2 --max-distance 16 bp; d…  [https://github.com/dohlee/metheor/blob/master/README.md + https://pmc.ncbi.nlm.nih.gov/art…]
    - Metheor distance is within-read between-CpG-position (same molecule); FDRP/qFDRP score a single genomic position's local concordance — all metrics are within-re…  [https://pmc.ncbi.nlm.nih.gov/articles/PMC10062925/ + https://pmc.ncbi.nlm.nih.gov/articles…]
    - Speedup up to 300-fold and memory up to 60-fold vs methclone, WSHPackage (R), and Perl scripts; benchmarked on Intel Xeon E7-4850 2.10GHz/512GB with simulated +…  [https://pmc.ncbi.nlm.nih.gov/articles/PMC10062925/]
    - Algorithm is one-sweep read-centric: ~n read accesses vs CpG-centric λ·n; input is Bismark-aligned bisulfite BAM with XM tags (short-read, e.g. 29nt CCLE RRBS),…  [https://pmc.ncbi.nlm.nih.gov/articles/PMC10062925/ + https://github.com/dohlee/metheor/blo…]

## DEEP: Long-Read POG advanced-cancer cohort paper — O'Neill, Pleasance, ... Jones (senior); "Long-read sequencing of an advanced cancer cohort resolves rearrangements, unravels haplotypes, and reveals methylation landscapes." Cell Genomics 4(11):100674, 2024. PMID 39406235, DOI 10.1016/j.xgen.2024.100674. Long-read ONT (PromethION), CANCER (advanced/metastatic), region-level + haplotype-level allelic methylation. The "tool" is actually a multi-component PIPELINE, not one binary: Clair3/WhatsHap (SNV phasing) -> NanoMethPhase (haplotag + methylation phasing) -> DSS (region DMR calling). All three verified from primary sources below.  (src: VERIFIED from primary sources: (1) Paper full text — PMC11605692 (https://pmc.ncbi.nlm.nih.gov/articles/PMC11605692/), t…)
  method_detail: ALGORITHM (verified, primary sources): (1) PHASING — SNVs phased with WhatsHap "as included with clair3" (Clair3 caller); plus NanoMethPhase's own read+methylation phasing. NanoMethPhase (Akbari et al., Genome Biol 2021, GitHub vahidAK/NanoMethPhase) takes a WhatsHap phased VCF (0|1 / 1|0) + per-read 5mC calls and assigns reads/CpG methylation to HP1/HP2. (2) METHYLATION CALLING — 5-methylcytosine (5mC) ONLY, called with nanopolish 0.13.3. NO 5hmC analysis (contrast: our ISM/MSA reads MM/ML and can separate 5mC vs 5hmC — and a known MSA bug double-counted 5mC+5hmC). (3) aDMR DETECTION — NanoMethPhase 'dma' module is a WRAPPER around the DSS B…
  vs_ISM_detailed: PEER, NOT COMPETITOR — different granularity, same biology. WHAT THEY CAN DO THAT ISM CANNOT: (A) RECURRENCE / cohort power — 189 tumors/181 patients let them call 2,854 genes (8,511 transcripts) with RECURRENT promoter aDMRs (RET in 26 samples = intragenic CGI alternate promoter; CDKN2A in 9 samples). Our work is single-sample (HCC1395) or 6-sample A-pilot where cross-sample somatic recurrence was 0/38 same-locus (mutations are private) — we structurally cannot establish gene-level recurrence at this scale. (B) CNV/LOH-AWARE genome-wide aDMR RATE — they quantify 4.46M aDMRs, mean 23.61K/tumor (SD 14.64K), ~5x the matched-normal rate (4.7K, SD 876), and show 79% map to heterozygous CNV/LOH. This is exactly the confound our memory flags (ASM…
  ISM_could_learn: 
  verified_claims (10):
    - Cohort = 189 tumor samples from 181 patients on ONT PromethION; 43 tumor samples from 41 patients have matched-normal nanopore sequencing. Median 17.5x (normal)…  [PMC11605692 (Cell Genomics 2024, PMID 39406235) full text — quoted '189 tumor samples from…]
    - An average of 84% of CpG sites per sample could be phased (median 86%, SD 7.1%); gene haplotypes resolved promoter-to-3' for a median 85% of genes (IQR 79.1-89.…  [PMC11605692 full text — 'an average of 84% of the CpG sites within each sample could be ph…]
    - Detected 4.46 million aDMRs across all tumors; mean = 23.61K per tumor (SD 14.64K), ~5-fold more than in matched normals (mean 4.7K, SD 876).  [PMC11605692 full text — 'We detected 4.46 million (mean = 23.61K, SD = 14.64K) aDMRs acros…]
    - 79% of aDMRs mapped to heterozygous copy-number-variant (CNV) and LOH regions.  [PMC11605692 full text — 'The majority (79%) of the aDMRs mapped to heterozygous copy numbe…]
    - 2,854 genes (8,511 transcripts) had recurrent promoter aDMRs, including cancer genes RET (intragenic CGI alternate promoter aDMR in 26 cohort samples) and CDKN2…  [PMC11605692 full text — 'We detected 2,854 genes (8,511 transcripts) with recurrent aDMRs …]
    - BRCA1, RAD51C and MLH1 findings are promoter HYPERMETHYLATION (HRD/Lynch context), not strictly aDMRs: BRCA1 promoter hypermethylation in 3 breast cancers; RAD5…  [PMC11605692 full text — 'BRCA1 promoter hypermethylation observed in three breast cancer s…]


# CROSS-ANALYSIS DIGEST


## CROSS: Δβ / methylation-difference effect-size methodology — is ISM's raw per-CpG mean Δβ (max-collapse any-mod, Fisher exact, no dispersion shrinkage, no smoothing) sound vs the field standard (DSS, BSmooth, M-value, modkit dmr, MeConcord)?  | align=PARTIAL. ISM aligns with the field on the descriptive layer … conflict=low
  external: The field has converged on variance-stabilized, dispersion-aware, model-based difference estimation rather than raw per-CpG mean differences. Five anchors, all primary-source verified:  1) DSS (beta-binomial GLM, Feng/Wu): reparameterizes counts by MEAN + DISPERSION; the dispersion is SHRUNK via a Bayesian hierarchical model pooling information across CpGs; DM tested by a Wald test per CpG; callDML supports a `delta` effect-size threshold (poster…
  ours: ISM computes Δβ as the per-CpG MEAN methylation difference between haplotype groups (HP1 vs HP1-1 somatic-controlled; ALT vs REF allele-axis), paired by CpG, tested with Wilcoxon signed-rank + Cohen d, on raw mod probabilities binarized at >0.8 / <0.2. Per-CpG significance uses Fisher exact + BH-FDR. Probabilities are max-collapsed across any-mod (…
  VERDICT: SOUND-FOR-PURPOSE WITH BOUNDED, KNOWN BIASES — not a flawed method, but its effect-size MAGNITUDE claims need a variance-stabilized cross-check.  The core architectural choice — read-to-read distance + clustering + PERMANOVA to test haplotype STRUCTURE, decomposed against a normal baseline to separate cis from copy/drift — is sound, novel relative to the published toolset (no standard tool combines these), and correctly targeted at a question (between-molecule organization under matched CN) that DSS/BSmooth/modkit do not address. On model compl…
  IMPROVE/LEARN: Concrete, prioritized fixes (each maps to a verified field method):  1) [HIGH, low-effort] Add an M-value / logit cross-check on headline loci (Du 2010). Recompute BRCA2 (−0.122) and TBC1D16 (+0.142) effect sizes as Δ(M-value) and confirm sign + significance survive variance stabilization. If a Δβ at the 0/1 extreme shrinks or flips under M-value, flag it. Cheap, directly addresses B1. Keep reporting Δβ as the human-facing number (Du's explicit recommendation) but let the M-value version carry the inference.  2) [HIGH] Stop max-collapsing 5mC+5…
  sources (7):
    - DSS / Feng-Wu beta-binomial + Wald + dispersion shrinkage; Fisher exac…: https://pmc.ncbi.nlm.nih.gov/articles/PMC8171293/ and https://bioconductor.org/packages/devel/bioc/vignettes/DSS/inst/doc/DSS.html — beta-bi…
    - Du et al. 2010 BMC Bioinformatics — M-value vs beta-value heteroscedas…: https://pmc.ncbi.nlm.nih.gov/articles/PMC3012676/ — M=log2(beta/(1-beta)); beta-value severe heteroscedasticity in [0,0.2] and [0.8,1], M-va…
    - BSmooth / bsseq — Hansen, Langmead, Irizarry 2012 Genome Biology: https://pubmed.ncbi.nlm.nih.gov/23034175/ — local-likelihood smoothing borrows strength across neighbouring CpGs to compensate for low cover…
    - modkit dmr (Oxford Nanopore native) — beta/Dirichlet likelihood-ratio,…: https://nanoporetech.github.io/modkit/dmr_scoring_details.html and https://nanoporetech.github.io/modkit/intro_dmr.html — methylation prob ~…
    - MeConcord — Oxford Bioinformatics 2022, read-to-read + CpG Hamming con…: https://academic.oup.com/bioinformatics/article/38/Supplement_1/i307/6617534 — pairwise Hamming distance across reads (RC) and across CpGs (…
    - PERMANOVA — Anderson; scikit-bio implementation: https://onlinelibrary.wiley.com/doi/full/10.1002/9781118445112.stat07841 and https://scikit.bio/docs/dev/generated/skbio.stats.distance.perm…

## CROSS: ASM magnitude & directional bias: our within-sample somatic-controlled read-rate vs cross-individual / haplotype-anchored DMR-rate literature (Do 2020; Martin-Trujillo 2017; POG/O'Neill 2024; Schalkwyk 2010; Sakamoto 2022)  | align=DIFFERENT CALIBER on the headline numbers (not a real confli… conflict=
  external: VERIFIED from primary sources:  [Do 2020, Genome Biology, PMC7322865] WGBS+SureSelect, 81+24 samples incl. 16 cancers. SNP-anchored, multi-sample DMR calling (ASM required in >=2 samples; >=3 CpGs, >20% methyl diff, FDR<0.05). 15,112 recurrent ASM DMRs = 0.7% of informative SNP-containing regions. Cancer ASM 5-9x higher than matched normal (5x overall p=1.7e-7; 5x myeloma / 8.5x lymphoma / 9x GBM; 7x excl. placenta). DIRECTION: hypomethylation (l…
  ours: ISM internal (HCC1395 SEQC2, single main sample, single ClairS-TO pipeline, tier 3): strong-ASM rare 0.34% genome-wide with NO directional bias (hypo 44% / hyper 56%). BRCA2 promoter HP-axis Delta-beta=-0.122 (n=197), ~80% copy-component + only marginal true cis d_within=-0.023 (perm p=0.022); chr17/TBC1D16 the only clean cis exemplar (d_within=0.1…
  VERDICT: DIFFERENT CALIBER (predominantly), NOT CONFLICT — with one low-powered partial tension on directionality. Our headline "rare 0.34% / no directional bias / BRCA2 hypo" does NOT contradict the literature once measurement frames are matched: (a) our 0.34% is a stringent within-sample somatic-controlled HP-axis read-rate vs the literature's cross-individual / haplotype-anchored DMR counts that bundle in germline cis-ASM + CN/LOH-driven allelic methylation (Do 2020 5-9x, POG 4.46M, 79% in CNV/LOH); (b) "no directional bias" is the expected signature…
  IMPROVE/LEARN: 1. FIX the citation: the prompt's "Sakamoto 2022 = patient-private hap-DMR" is imprecise. Sakamoto et al. 2022 (Nat Commun 13:3464) is a lung-cancer long-read PHASING paper (its hap-DMR metric is UNVERIFIED here); the clean primary anchor for "patient-private vs recurrent hap-DMR in cancer (ONT, haplotype-anchored)" is O'Neill/POG 2024 (Cell Genomics, PMC11605692). The genome-wide nanopore "200 allelic DMR / LCL / germline+somatic" work is Akbari NanoMethPhase 2021 (Genome Biology, PMC7898412), germline not cancer. Re-cite accordingly before an…
  sources (6):
    - Do 2020, Genome Biology 21:153, PMC7322865, DOI:10.1186/s13059-020-020…: VERIFIED primary: 15,112 recurrent ASM DMRs = 0.7% of informative SNP-containing regions; cancer ASM 5-9x higher than matched normal (5x ove…
    - Martin-Trujillo 2017, Nat Commun 8:467, PMC5589900, DOI:10.1038/s41467…: VERIFIED primary: 306 cancer cell lines + 178 TCGA, 450K + SNP6 arrays, 661 probes at 37 imprinted DMRs. In CN-normal samples aberrant impri…
    - O'Neill / POG 2024, Cell Genomics, PMC11605692, DOI:10.1016/j.xgen.202…: VERIFIED primary: ONT PromethION long-read, 189 tumors/41 normals, haplotype-anchored (NanoMethPhase, 84% CpGs phased). 4.46M aDMRs, ~5x tum…
    - Schalkwyk 2010, AJHG 86:196-212, PMC2820163, PMID:20159110: VERIFIED primary: Affymetrix SNP6 + MSRE, 10 germline individuals, 7.6% of genome. 2,704/183,605 SNPs (1.5%) RAS>=0.10 => >35,000 cis-ASM ex…
    - Sakamoto 2022, Nat Commun 13:3464: PARTIAL/UNVERIFIED: confirmed to exist as ONT long-read lung-cancer phasing paper, but specific patient-private hap-DMR metric NOT fetched f…
    - Akbari NanoMethPhase 2021, Genome Biology 22:68, PMC7898412 (clarifica…: VERIFIED primary: genome-wide nanopore ASM, 12 LCLs, ~200 allelic DMRs, germline+somatic novel imprinted regions — GERMLINE LCLs not cancer;…

## CROSS: ISM "structure-not-disorder" framing (PERMANOVA on read-read methylation distance matrix testing haplotype/allele-LABELED between-molecule separation) vs the WSH/"disorder" literature (PDR, epipolymorphism, methylation entropy/informME, FDRP/qFDRP).  | align=STRONGLY ALIGNED on the central thesis, one framing correcti… conflict=low-conflict / strongly-corrob…
  external: VERIFIED PRIMARY SOURCES. (1) Landau 2014 PDR (Cancer Cell, CLL, PMC4302418): PDR = within-read Proportion of Discordant Reads, >=4 CpGs/read, >10x cov. EXPLICITLY tested and REJECTED ASM as the source ("discordant reads involve both parental alleles"; >2 patterns/locus rules out 1-2 ASM patterns); disorder = "stochastic genome-wide process". Short-read WGBS/RRBS, primary cancer. (2) Landan 2012 epipolymorphism (Nat Genet ng.2442, PMID 23064413):…
  ours: ISM (per verified C++ brief): read x CpG matrix -> read-read pairwise distance (NHD/L1/Bernoulli/Pearson/Jaccard, min 5 common CpG, NaN->MAX_DIST) -> PERMANOVA (pseudo-F, 999 perms, SS_within vs SS_between) + dispersion test, testing whether PRE-LABELED read groups (HP1 vs HP1-1 somatic-controlled; ALLELE = ALT vs REF; somatic vs germline) are STRU…
  VERDICT: SUPPORTED with one framing correction. ISM's PERMANOVA-on-read-read-distance genuinely measures a different quantity (supervised haplotype/allele-labeled between-molecule STRUCTURE) than the disorder lit's unsupervised, explicitly-stochastic, non-allelic WSH. Landau 2014's explicit ASM-rejection is the single strongest external corroboration. CAVEAT 1: do NOT frame as "within-read (disorder) vs between-read (ISM)" — Scherer 2020 shows epipoly/entropy/FDRP/qFDRP are all inter-molecule; frame as label-tested structure vs label-free entropy. CAVEA…
  IMPROVE/LEARN: (1) ADD qFDRP as the coverage-robust DISORDER contrast (Scherer recommends it; between-read like ISM but unsupervised/scalar). Cleanest head-to-head: same pairwise primitive, qFDRP = label-free disorder scalar, ISM keeps matrix + PERMANOVA on HP/somatic label. 2x2: high qFDRP + non-sig PERMANOVA = stochastic erosion; high qFDRP + sig PERMANOVA = structured allelic ASM. Strongest single add; directly operationalizes "structure not disorder". (2) Swap CORE4 epipolymorphism for qFDRP in the contrast slot — epipoly is the coverage-fragile score tha…
  sources (6):
    - Landau2014_PDR: Cancer Cell CLL, PMC4302418. PDR = within-read Proportion of Discordant Reads, >=4 CpGs/read, >10x. EXPLICITLY tested+rejected ASM ('discord…
    - Landan2012_epipoly: Nat Genet ng.2442, PMID 23064413. Epipolymorphism = prob two random 4-CpG epialleles differ across reads. DMR formation = 'stochastic proces…
    - Scherer2020_WSH: NAR 48(8):e46, PMC7192612. INTER-molecule = epipoly/entropy/FDRP/qFDRP; INTRA-molecule = PDR/MHL. qFDRP = continuous Hamming distance betwee…
    - Jenkinson2017_informME: Nat Genet 2017 49(5):719-729 ng.3811. Quantifies 'methylation stochasticity genome-wide using Shannon entropy' via Ising potential-energy la…
    - Internal_O11: VERIFIED internal (memory project_O11_heterogeneity_negative): epipoly AUC 0.845 raw -> 0.530 residualizing n_reads+num_cpgs; r=0.79 with re…
    - informME_github: GitHub GarrettJenkinson/informME confirms WGBS information-theoretic pipeline (Shannon entropy, NME). https://github.com/GarrettJenkinson/in…

## CROSS: methylation-locus-association axis (within-read CpG-CpG co-methylation/LD, between-read read-read distance + clustering, cohort CpG-CpG correlation networks) vs ISM  | align=COMPLEMENTARY-WITH-A-GENUINE-UNFILLED-NICHE. The three exter… conflict=
  external: VERIFIED FROM PRIMARY SOURCES. The external "methylation-locus-association" literature splits into 3 distinct axes, and NONE occupies ISM's exact cell.  AXIS (a) WITHIN-READ CpG-CpG co-methylation / LD (intra-molecule): - MHB/MHL (Guo/Shen, Nat Genet 2017, PMID 28263317): defines Methylation Haplotype Blocks via LINKAGE DISEQUILIBRIUM between ADJACENT CpGs computed from individual reads (147,888 blocks); MHL = conservation of consecutively-methyl…
  ours: ISM (verified from C++ source per task brief) occupies AXIS (b) — between-molecule read-read DISTANCE + clustering — but with a configuration no verified external tool matches: - CORE 1: explicit read-to-read pairwise DISTANCE matrix (NHD default, +L1/Bernoulli/Pearson/Jaccard; min common CpG=5; NaN→MAX_DIST; strand-aware) — distance-based, vs cvlr…
  VERDICT: DEFENSIBLE NICHE, NARROW BUT REAL. ISM's individual ingredients are NOT novel in isolation: read-read clustering for ASM exists (cvlr); within-read co-methylation/LD exists (MHB/MHL, Metheor); haplotype DMR calling exists (NanoMethPhase, pycoMeth); the disorder family (NME/epipolymorphism/PDR) is standard (Metheor). The genuinely unfilled combination, verified against primary sources, is the COUPLING of: read-read distance matrix (multi-metric, distance-based not model-based) + PERMANOVA/dispersion structural-significance test + normal-anchored…
  IMPROVE/LEARN: 1. CITE cvlr as the nearest-neighbor and articulate the delta explicitly: ISM = distance-based (UPGMA + silhouette + multi-metric) + PERMANOVA significance vs cvlr = model-based Bernoulli-mixture EM with fixed k and NO significance test. The PERMANOVA-on-read-read-distance significance test is the most defensible single novelty — foreground it. 2. The PERMANOVA "structure not disorder" engine answers a question the entire disorder family (Metheor PDR/LPMD/FDRP/NME/epipolymorphism, which ISM already computes in CORE 4) cannot: are read groups ST…
  sources (8):
    - PMID 28263317 (Guo/Shen, Nat Genet 2017, ng.3805): MHB defined by LD between adjacent CpGs from individual reads (147,888 blocks); MHL = within-read haplotype conservation; SHORT-READ (61 WGB…
    - Metheor — PLoS Comp Biol 2023, 10.1371/journal.pcbi.1010946; PMC100629…: Rust on Bismark-tagged BAM = SHORT-READ. Reconciled: PDR/LPMD/MHL within-read (LPMD = CpG pairs on SAME read, length-bias-free, n_disc/(n_co…
    - cvlr — Raineri et al., Bioinformatics Advances 2023, 3(1):vbac101: ONT long-read; EM on mixture of multivariate Bernoulli over reads x positions binary matrix; user-set k; reports median absolute methylation…
    - CoMeBack — Bioinformatics 2020, 36(9):2675: ARRAY co-methylated regions (CMRs): chain adjacent probes <2kb, CpG density >=1/400bp, R2>0.3 ACROSS INDIVIDUALS. Cohort/cross-sample axis (…
    - Comethyl — Mordaunt et al., Brief Bioinform 2022, 23(2):bbab554; githu…: WGCNA on region methylation across samples → eigennode modules vs traits. SHORT-READ WGBS, cross-sample correlation network axis (c), not re…
    - NanoMethPhase — Genome Biol 2021, 10.1186/s13059-021-02283-5; PMC78984…: ONT; Clair SNV → WhatsHap phase → per-haplotype methylation FREQUENCY → DSS DMR. Explicitly does NOT compute distance matrices, cluster read…

## CROSS: Literature cross-validation of two ISM internal conclusions: (1) methylation->TP/FP somatic variant filter is NEGATIVE and ML-circularity is a recognized pitfall; (2) methyl-assisted phasing belongs in the LOH/cancer whitespace, since published methyl-phasing tools are germline-only while LOH-phasing tools use CNA/SV/Hi-C, not methylation. Context = ONT long-read CANCER somatic methylation (region/read/haplotype-level).  | align=STRONGLY ALIGNED on both, with one nuance. Conclusion 1: Ful… conflict=LOW — no contradiction with an…
  external: CONCLUSION 1 — methyl->FP filter NEGATIVE; ML-circularity is recognized. (a) NO published somatic small-variant caller uses methylation as a standalone FP filter. Verified from primary sources for every major long-read/short-read caller:   - ClairS-TO (Nat Commun 2025, s41467-025-64547-z; GitHub HKU-BAL/ClairS-TO; our own pipeline): FP filtering = 9 hard-filters + 4 PoNs (gnomAD/dbSNP/1000G/CoLoRSdb) + "verdict" module that statistically separate…
  ours: ISM internal (HCC1395 SEQC2 truth, tier 3, single ClairS-TO pipeline, ONT long-read 5mC/5hmC, cancer somatic): C1) methyl->TP/FP variant filter NEGATIVE four ways: LOSO 100% sample-level circularity; AUC=0.505 (chance); strong-ASM anti-discriminative (enriched in FP, OR=0.194); methylation 5th-rank vestigial. Supporting ASM characterization: strong…
  VERDICT: BOTH CONCLUSIONS SUPPORTED by primary-source literature.  (1) methyl->TP/FP filter NEGATIVE: VERIFIED-CONSISTENT. No published somatic caller uses methylation as a standalone FP filter (ClairS-TO/ClairS+LongPhase-S/DeepSomatic all verified to use PoN/tumor-purity-CN/haplotype-consistency/pileup-CNN instead). ML/sample-level circularity (our LOSO finding) is a formally recognized pitfall (Soneson 2014 PLOS ONE; Bernett 2024 Nat Methods; Kapoor-Narayanan survey). Our NEGATIVE is a clean negative result on an unattempted idea, not a contradiction …
  IMPROVE/LEARN: PHASING-WHITESPACE IMPROVEMENT PLAN (how to convert the LOH-phasing positive into a defensible, novel cancer contribution):  1. POSITION as the explicit gap-filler, not a new method-class. Frame = "germline methyl-phasing (MethPhaser/HapBridge) explicitly stops at LOH/cancer; cancer LOH-phasers (Wakhan/HiCancer) use CNA/SV/Hi-C and ignore methylation. We test whether haplotype-specific methylation rescues read-to-haplotype assignment in cnLOH blocks where SNV heterozygosity is gone." This is the precise unoccupied cell and is directly citable a…
  sources (12):
    - ClairS-TO (Nat Commun 2025) s41467-025-64547-z + GitHub HKU-BAL/ClairS…: FP filtering = 9 hard-filters + 4 PoNs (gnomAD/dbSNP/1000G/CoLoRSdb) + 'verdict' germline-somatic separation via tumor purity + CN profile +…
    - ClairS + LongPhase-S (biorxiv 2023.08.17.553778): FP post-filter reconstructs somatic haplotypes + infers tumor purity, flags haplotype-inconsistent variants as LowQual; NO methylation. (lon…
    - DeepSomatic (Nat Biotech 2025) s41587-025-02839-x: DeepVariant-derived pileup-tensor CNN, short+long-read somatic SNV/indel; NO methylation feature mentioned. (both platforms, somatic)
    - Soneson, Gerster & Delorenzi 2014 PLOS ONE e100335 (PMID 24967636): Batch/sample-composition confounding strongly inflates cross-validation performance estimates — canonical statement of the sample-level circ…
    - Bernett et al. 2024 Nature Methods s41592-024-02362-y (pub 2024-08-09): Naive random CV leaks info train<->test giving inflated metrics; recommends group-aware splitting (DataSail/GraphPart). Group/leave-one-grou…
    - Kapoor & Narayanan leakage survey (cited via s41592-024-02362-y; meta-…: Leakage affects hundreds of papers across 17 fields; genomic feature-selection leakage inflates AUROC ~0.18 avg. Pitfall existence VERIFIED;…


# SYNTHESIS

## TOP IMPROVEMENTS:
- **Adopt per-CpG beta-binomial + lognormal dispersion shrinkage to replace Fisher exact, giving Δβ a model-based SE/CI** (effort=High — re-implement beta-binomial+lognor…; src=DSS (Feng/Conneely/Wu 2014 NAR; Wu 2015 pseudo-replicate). ISM-side verified: Pe…)
    VERIFIED gap: ISM's per-CpG test is Fisher exact on a 2x2 (PerCpgAsm.cpp) with NO overdispersion model and NO cross-site stabilization; the only 'dispersion' in the codebase is the PERMANOVA centroid-homogeneity ANOVA-F (StructureTest), unrelated to count overdispersion. Long reads from the same haplotype/subclone are non-independent, so Fisher treats correlated Bernoulli draws as independent -> v…
- **Build the read-read distance matrix on SOFT methylation probabilities instead of hard >0.8/<0.2 binarization** (effort=Medium — add a soft-distance option alon…; src=cvlr (Raineri 2023 Bioinformatics Advances, multivariate-Bernoulli EM); pycoMeth…)
    VERIFIED: DistanceMatrix.hpp:27-28 binarizes every CpG to methylated/unmethylated/ambiguous before NHD/Jaccard, discarding ML/mod_qual confidence. cvlr's Bernoulli-mixture and pycoMeth's BernoulliPosterior both keep p(methylated). Propagating soft probabilities into the matrix reduces low-coverage noise (the regime where ISM penalizes pairs to MAX_DIST=1.0) and lets PERMANOVA/clustering weight hig…
- **Add a directed HP-stratified CpG-PAIR (tuple) co-methylation log-odds score complementing per-CpG Fisher — exploiting the long-read advantage DAMEfinder lacks** (effort=Medium — weighted log-odds per CpG-pair …; src=DAMEfinder (Orjuela et al. 2020 Epigenetics & Chromatin — NOTE both the prompt's…)
    VERIFIED gap: no tuple/cpg-pair/co-methylation/concordance code in src/core+include; ISM captures on-read structure ONLY via unsupervised NME/epipolymorphism, with no DIRECTED HP1-vs-HP1-1 concordance statistic. DAMEfinder's core insight is on-read MM/UU vs MU/UM concordance carries allele-specificity single-CpG tests miss. ISM has the ideal substrate (full read×CpG matrix; long reads span >>150bp…
- **Use modkit dmr's Beta-Binomial likelihood-ratio as a fast genome-wide PRE-FILTER before the expensive O(N^3) UPGMA + 999-perm PERMANOVA** (effort=Low-Medium — modkit is a single Rust bin…; src=modkit dmr (ONT, Beta-Binomial marginal LR + MAP p-value). MHL screen (Guo 2017)…)
    ISM's structural engine (cores 1-3) is heavy: UPGMA is O(N^3) per region, PERMANOVA runs 999 label permutations. modkit dmr is a fast count-based rate-difference test overlapping ISM's β-rate/Δβ layer. Running modkit dmr genome-wide first to triage candidate regions, then reserving the structural test for regions passing a rate-difference or MHL-coordination screen, cuts compute without changing c…
- **Add Cohen's h (arcsine) as a saturation-robust companion to Cohen's d on Δβ, and keep 5mC/5hmC distinct via Dirichlet-Multinomial instead of max-collapse** (effort=Low for Cohen's h (one formula in Python…; src=modkit (Cohen's h; Dirichlet-Multinomial). Du et al. 2010 (beta vs M-value). Ver…)
    Two coupled effect-size fixes. (a) Cohen's d on raw β is unstable at saturating coverage — targeted cancer loci often hit near-100% methylation in both groups where raw β-difference compresses; Cohen's h (arcsine variance-stabilized) is robust and is modkit dmr's native effect size. (b) ISM MAX-collapses 5mC+5hmC into 'any-modification' (cis_asm_core.py:31-32) — VERIFIED this exact double-count ca…
- **Add an imprinting/germline-ICR POSITIVE control and reframe ASM as cis/trans-regulatory (not a variant filter), borrowing the germline benchmarks of pycoMeth/cvlr/NanoMethPhase** (effort=Low-Medium — pick a canonical imprinted …; src=pycoMeth GIAB imprinting (Snajder 2023); cvlr NA12878 allele-specific (Raineri 2…)
    Every read-level ASM/clustering competitor (cvlr, pycoMeth, NanoMethPhase, DAMEfinder) is VALIDATED on germline imprinting/parent-of-origin (GIAB trio, NA12878). ISM has NO known-truth positive control — only its HP-shuffle null. Running ISM on a canonical imprinted ICR (where the answer is established) gives a positive control that the structural engine detects real ASM when it exists, strengthen…

## NOVELTY DEFENSIBLE:
- THE NARROW COMBINATION ISM UNIQUELY OWNS (digest correctly isolates it): an explicit N×N read↔read DISTANCE matrix (cores 1-2, NHD/UPGMA/silhouette) + a non-parametric PERMANOVA STRUCTURE test (core 3, 999 label-permutations on that matrix) applied to SOMATIC cancer haplotype groups with a NORMAL-AN…
- PERMANOVA-AS-'STRUCTURE-NOT-DISORDER' is genuinely distinctive: the survey establishes the entire disorder family (entropy, PDR, epipolymorphism, NME, FDRP/qFDRP) answers 'how random is methylation within a molecule' while ISM cores 1-3 answer 'do molecules partition into structurally-separated subp…
- THE NORMAL-ANCHORED SOMATIC cis-CONTROL is the single capability NO other ASM/phasing tool has: every competitor (DAMEfinder, NanoMethPhase, pycoMeth, methylpy AMRfinder, modkit dmr) is a symmetric group-A-vs-group-B or trio test that CANNOT separate somatic-specific allelic methylation from baselin…

## GAPS MISSING:
- NO actual head-to-head benchmark for ANY tool except a partial modkit pilot. VERIFIED: modkit not on PATH, no ~/.cargo, no DSS/cvlr/DAMEfinder/pycoMeth binaries on disk — every comparison except modkit is ON-PAPER conceptual mapping. The ONE exception (scripts 54-56) is single-sample HCC1395 A-pilot…
- cvlr's primary source (GitHub repo + exact CLI/implementation language) is UNVERIFIED — the dossier explicitly notes github.com/EmanueleRaineri/cvlr returns 404 and the author account shows 0 public repos as of the survey date. Any claim about cvlr's flags, missing-data M-step, or k-fixing is from t…
- The delta-beta methodology category (10th/final bucket) is TRUNCATED mid-entry (cuts off inside the Metheor deep-tool dossier). The full effect-size/transform competitor set (arcsine vs logit, limma moderated-t, M-value vs beta) is listed but its head-to-head against ISM's Cohen's d/Wilcoxon is inco…
- ASMS (Raineri 2024, ONT modBAM read-level clustering -> ASM candidates) is the CLOSEST published analog to ISM (long-read, read-level, no-phasing clustering for ASM) yet appears ONLY as a one-line ASM-caller entry with NO deep dossier. This is the most important missing comparison — if any tool thre…
- NO runtime/scalability/compute comparison anywhere. ISM's UPGMA is O(N^3) per region + PERMANOVA 999 permutations; modkit/DSS are speed-designed. The survey compares STATISTICAL constructs but never the practical cost axis (genome-wide feasibility, memory, wall-clock) that determines whether ISM's s…
- Two CITATION errors flagged but not fully resolved: DAMEfinder mis-cited in BOTH the prompt (wrong journal) AND ISM's source comment (PerCpgAsm.cpp:6 'De Waele 2020'; correct = Orjuela et al. 2020). Landau 2014 PDR primary text was CAPTCHA-blocked (algorithm wording from secondary re-implementations…
- Single-cell methylation tools (MethSCAn, epiCHAOS) appear only as one-liners. If ISM extends to single-cell or claims cell-level vs molecule-level resolution, the sc-bisulfite clustering literature (which DOES do model-based read/cell clustering with k-selection) is under-surveyed.
- The survey establishes ISM's cores are distinctive but does NOT quantify whether the distinctiveness MATTERS empirically — on a region where modkit dmr says 'no rate difference' but ISM PERMANOVA says 'structured', is the ISM call biologically real or noise? No worked discordant-case exists, so 'ISM…

## COMPLETENESS: 