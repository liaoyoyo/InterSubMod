<!--
建立時間: 2026-06-09
狀態: tasks (要確認的任務 + 細節任務樹 — 有向)
報告類型: paper_focus_task_tree
受眾: 廖子游 · 其他 AI session
provenance_note: 任務來自 03 方向卡裁決；分 confirm/validate/code/provenance 四類；無新數字。
-->
<!-- provenance-verified: 任務由 03_研究方向卡 裁決導出；本檔為計劃導航。 -->

# 確認任務樹（要確認的任務 + 細節子任務，有向）

> **L0 一眼結論**：要做的事分 4 類 —— **CONFIRM（你拍板的 framing 決策，0 算力）** → **CODE（ISM 方法精修，要寫碼）** → **VALIDATE（要跑分析驗證）** → **PROVENANCE（投稿前補原檔對賬）**。**最該先做＝CONFIRM-1（共驗證定義）+ CODE-catalog（位點標籤目錄）**，兩者解鎖最多下游又不卡 HD-1。
>
> **L1 重點邏輯（依賴關係）**：
> ① CONFIRM 類**不卡任何東西、先做**（純拍板，定 framing）。
> ② CODE-catalog（ISM-4）是**價值最高的單一交付物**，把零散觀察變論文表，且只依賴既有 run。
> ③ VALIDATE 類多數 **off 關鍵路徑**（HD-4 closeout / umtag yardstick 都要 COLO829）。
> ④ HD-1（phasing GO/NO-GO）是**獨立決策閘**，不在這棵樹（在執行 DAG），但 gates phasing 段 grade。

> 標記：🔴 決定性｜⭐ 高價值｜◽ 一般｜[依賴: X] 表示要先做 X。

---

## 分支 A — CONFIRM（你拍板，0 算力，先做）

- **CONFIRM-1** 🔴 ⭐ **「共驗證」定稿 = 正交佐證（co-validation）不是互相篩選（filter）** —— 確認標題與全篇用 (A) 版定義。[解鎖: 全篇 framing]
- **CONFIRM-2** ⭐ 確認 **D4/D6/D5/困難-1 四個方向 NO-GO**（接受甲基側 = phasing/germline-allelic/circular，不再投 filter 用法）。
- **CONFIRM-3** ◽ 確認**重要目標 Tier**（見 `06`）：核心 = characterization + NEGATIVE 底座 + 位點 catalog；潛力 = umtag + cis 協議。

## 分支 B — CODE（ISM 方法精修，要寫碼；先記錄，未來執行）

- **CODE-catalog** ⭐ **位點甲基分群標籤 catalog（ISM-4）** —— 定 schema（reliable/latent/none × Δβ × somatic/germline/drift × cis/copy/untestable × tier）→ 組表。[依賴: 既有 6-sample ISM run；建議優先]
- **CODE-cramersv-audit** 🟡 **CramersV reliability gate 過嚴審查（ISM-2）** —— /methodology-audit RegionProcessor.cpp:1592；評估二元 0/1 → 三態（reliable/latent/unreliable）。[Hard Gate: C++ 改動需 /cpp-change + 編譯]
- **CODE-merge-dbeta** ⭐ **合併 Δβ 互補發現通道（ISM-3）** —— ISM 輸出加 per-position Δβ + union 標記（discrete-clustered OR large-Δβ-with-clustering）。[依賴: CODE-catalog schema]

## 分支 C — VALIDATE（要跑分析；多數 off 關鍵路徑）

- **VALIDATE-hd4-closeout** ◽ **N9 ISM re-run** 匯出 `fine_group_counts{HP1,HP1-1,HP2,HP2-1}` + `n_clusters`（甲基-only count）→ AF↔NGroups partial-corr knockout 補刀。[off 關鍵路徑；HD-4 三證已 RESOLVED]
- **VALIDATE-cis-protocol** 🟡 **cis-candidate 大規模驗證協議（D3）** —— normal-anchored cis-test 規模化 + copy-partition + **anchor SEQC2 CN** + 對 untestable（pure-ALT/CGI-desert）設計判別。[依賴: COLO829 解 untestable]
- **VALIDATE-umtag-yardstick** 🟡 **umtag yardstick（D1）** —— switch-error/N50 vs MethPhaser/HapBridge + 真-unphase 救援實測（非 held-out）+ orthogonal phase-truth。[依賴: COLO829 N8]
- **VALIDATE-colo829** ⭐ **COLO829 ⭐4 升級（N8）** —— 一投資解 ASM ⭐3→⭐4 + 給 umtag/cis phase-truth。[解鎖: cis-protocol + umtag-yardstick]

## 分支 D — PROVENANCE（投稿前必補原檔對賬；見 `02` 🟡 清單）

- **PROV-1** 🟡 定位 `condition_fp_consensus.json` 正確 emitted 檔名 → grep OR 8.63/4.09/5.84/0.194。
- **PROV-2** 🟡 cycle2 transfer 輸出 grep −0.377。
- **PROV-3** 🟡 上游 TSV grep null ARI 0.177。
- **PROV-4** ✅（2026-06-09 結案）chr17 perm p=0.001 + BRCA2 d_within −0.023 ← `survivor_permutation.json` + `copy_partition_confirm.json`。BRCA2 = **subclone/copy 主導**（HP1-1=subclone tag 非 copy、非 CN-dosage；**% 不 robust** 勿寫 ~80%）。
- **PROV-5** 🟡 *_gwasm.json grep 6/6 excess +0.101–0.241。
- **PROV-6** ◽ /citation-verification 核外部 PMID/DOI（Kapoor2023 / Martin-Trujillo2017 / O'Neill2024 等）。

---

## L1 — 建議執行序（不卡 HD-1）
1. **CONFIRM-1/2/3**（拍板，今天）→ 2. **CODE-catalog**（最高價值交付物）→ 3. **CODE-merge-dbeta + CODE-cramersv-audit**（方法精修）→ 4. **PROVENANCE 補對賬**（投稿前）→ 5. **VALIDATE-colo829** 解鎖 cis/umtag（資源到位再做）。

> 🔴 **HD-1（phasing R-SELFREF GO/NO-GO）獨立於此樹**，由你決定（→ 執行 DAG `docs/plans/20260608_…`）。
> 機械態 session 任務已用 TaskCreate 登錄（confirm/code/validate/provenance）。
