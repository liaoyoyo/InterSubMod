<!--
建立時間: 2026-08-09
目標: 盤點 InterSubMod 整體狀況、真正的 live 工作、證據鏈完整度，以及後續必做檢核與驗證
處理範圍: Task Type B — 全 repo 狀態與既有全量證據稽核；不啟動全基因組、大 BAM 或其他長計算
關聯檔案:
- InterSubMod/docs/CURRENT_FOCUS.md
- InterSubMod/docs/experiments/INDEX.md
- InterSubMod/state/active.json
- InterSubMod/state/focus.json
- InterSubMod/research/autoresearch/evidence_ledger.jsonl
- InterSubMod/docs/handoff/20260801_exactPS_readAF_CNV_AI交接_01/authority_manifest.json
- InterSubMod/output/OUTPUT_INDEX.md
-->

# 2026-08-09 整體狀況與檢核驗證盤點

用 SCQA：**現有 Exact-PS／read-level methylation 技術證據鏈仍可信，但目前 repo 尚未達正式 release-ready；真正的即時工作已轉到 lineage-tag 串接與解釋中心，而 live state、handoff、ledger 與索引沒有同步。（影響：高，信心：高）**

> **範圍標記 — EVIDENCE AUDIT / NO FULL RECOMPUTE**  
> 本輪覆蓋 repo、state、Git、manifest、receipt、ledger、輸出索引與現存測試 binary；重新計算 19 件 authority hashes，但沒有重跑全基因組、全 BAM、10,717 abstain second pass 或 Hypercube M2。既有全量結果只依 frozen receipts 判讀，不把本輪提升成新的科學 validation run。

## 1. 一句話總判定

**目前是「科學 baseline 可用、claim 必須受限；工程可操作但不可發布；治理 SoT 明顯漂移；長計算暫停」的狀態。**

| 面向 | 狀態 | 判定 |
|---|---|---|
| Exact-PS strict linkage | ✅ 已完成 7 technical datasets × chr1–22 | 可稱 local strict read linkage |
| Exact-PS topology + read-AF | 🟡 frozen technical pass with abstain | 可稱 model-conditional candidate topology；不可稱 biological tree |
| Methylation | 🟡 validated association-only | 可稱 read-level epigenetic association；不可稱 clone／ancestry／causality |
| 2026-08-01 observation report | ✅ `VALIDATED_DERIVED_OBSERVATION` | 報告衍生層通過，不提升底層證據等級 |
| all-sSNV Task-B | 🟡 data analysis validated；🔴 signed release stopped | 不可稱 final signed release |
| Hypercube likelihood v2 | 🔴 原始 NO-GO；修復後仍 PROBE | 不可稱 full likelihood validation |
| 工程 release readiness | 🔴 未過 | 磁碟、dirty tree、clean HEAD build、environment lock、full Python receipt 未完成 |
| live state / navigation | 🔴 不一致 | `CURRENT_FOCUS`、registry、ledger、handoff 與 output index 必須 reconcile |

## 2. 現在實際在處理什麼

需分三層閱讀，否則會把「已驗證 baseline」「程式主軸」與「最近交付」混成同一件事。

### 2.1 科學 canonical baseline

- 2026-08-01 Exact-PS 全 7 technical datasets observation 已驗證。
- 主要 claim ceiling 仍是：CN/LOH/purity/multiplicity/CCF 未整合；methylation 只能 association-only；confirmed cellular subclone、linear ancestry 均為 0。
- 2026-07-24 all-sSNV 科學分析已完成，但正式結果／報告的 authority、receipt、signature 未完成，維持 bounded stop。

### 2.2 現行程式主軸

- branch：`feat/lineage-tag-methylation-axes`。
- 核心方向：LongLineage lineage tags 進入 InterSubMod，針對 HP／ALT／lineage 多軸做 methylation PERMANOVA；對應核心 commits 為 `3ac07d3c`、`b6f9b521`。
- 但 repo 內尚找不到能完整綁定 A/B/C 路線、forest vs. single tree、tagged BAM 產物契約與 claim ceiling 的正式 decision authority。

### 2.3 最近即時工作

2026-08-08～09 最近 commits 主要是解釋中心、Wiki、GitHub Pages、SVG／視覺 QA 與外部 claim 修正；HEAD 為 `4de8b1d0`。因此「當下正在做」較精確的說法是：

1. 把既有科學與系統能力說清楚並發布；
2. 修正外部過度宣稱；
3. 完成 lineage-tag 串接後的可解釋介面；
4. 尚未啟動新的全量科學運算。

## 3. 全量科學證據目前支持到哪裡

### 3.1 Aggregate

- all-sSNV full scope：469,849 records。
- Exact-PS：98,955 groups；85,941 mutation-bearing；75,224 family complete；10,717 resource abstain。
- 71,955 ranked units：39,648 unique winner、23,858 same-topology tie、8,449 cross-topology tie。
- current strict read linkage 已完成 7/7 technical datasets；production cellular clone、exact biological parent-child、linear ancestry 仍為 0。
- multisite methylation：3 個 robust long-signature associations，但 formal full-four RR/RA/AR/AA = 0、same-locus cross-sample replication 未建立。

### 3.2 Canonical scope

- 7 technical datasets、6 biological samples、chr1–22；HCC1395 與 HCC1395_DORADO 是同一 biological cell line 的兩個 technical products，不可當兩個獨立生物重複。
- 2026-08-01 authority manifest 的 13 artifacts + 1 frozen binary + 5 source snapshots，2026-08-09 重算 **19/19 MATCH、0 missing、0 mismatch**。
- denominator registry 19/19、百分比與 7 條 conservation checks 均可重算通過。

### 3.3 Extreme outlier

- H2009 佔 resource abstain 8,457/10,717 = 78.9120%。因此缺失並非隨機，不能只用 cohort overall completion rate 推論每個樣本同等可靠。

### 3.4 Well-explained case

- read-AF 是 HP-specific supported-pattern ALT/(REF+ALT) 的 deterministic ordering score，不是 caller VAF、CCF、likelihood、posterior 或 clone prevalence。
- `validation_evidence_eligible=false` 仍在 cohort receipt；technical frozen pass 不等於 biological lineage validation。

## 4. 2026-08-05 四項工程阻塞的最新重判

| Gate | 08-05 | 08-09 實測 | Verdict |
|---|---:|---:|---|
| pytest 可執行 | 不可用 | canonical Miniconda Python 有 pytest 8.4.2；15/15 smoke PASS | ✅ PASS（只通過可執行窄 gate） |
| `/big7_disk` 可用空間 ≥2T | 617G | 617G、99% used | 🔴 FAIL |
| working tree clean | 580 項 | 稽核基線 16 項：6 modified、2 deleted、8 untracked；本報告另新增 1 個 untracked audit artifact | 🔴 FAIL |
| upstream divergence = 0 | ahead 268 | ahead 0 / behind 0；remote tip = local HEAD | ✅ PASS |

補充：`/bip7_disk` 只剩 1.2T、96% used，也不適合把大運算轉移過去。

## 5. Build / test 現況

### 5.1 已實跑

- `./build/bin/run_tests`：exit 0，**270/270 PASS，39 suites**；binary SHA-256 `e8ac8030…19bb`。
- `./build_handoff_verify/bin/run_tests`：exit 0，**265/265 PASS，38 suites**；缺目前 build 內 5 個 `RunSummaryJsonTest`。
- `tests/test_drilldown_contract.py`：pytest **15/15 PASS**。
- 2026-08-01 report build/finalize/QA：unittest **12/12 PASS**。
- readiness：PASS 14 / WARN 6 / FAIL 0。

### 5.2 尚不能宣稱 current-HEAD clean build

- 現在 HEAD：`4de8b1d0ba0cbe67c4b4a21506648c0e51276500`。
- `build/` identity：`0f9bb8dd-dirty`。
- `build_handoff_verify/` identity：`19c6b4bf-dirty`，且乾跑顯示 runner 尚需 37 個動作、主程式尚需 21 個動作。
- `build/` 對目前 source 乾跑為 0 動作，說明現存 source target 沒有待編，但 embedded Git identity 仍不是 current clean HEAD。

因此 270 tests 可證明「現存 binary 行為通過」，不能當作「目前 HEAD 可由乾淨環境 byte-reproducibly 重建」的 release receipt。

### 5.3 Python reproducibility

- canonical interpreter：`/bip7_disk/liaoyoyo2001/miniconda3/bin/python3`，Python 3.9.12，pytest 8.4.2。
- `/usr/bin/python3` 仍沒有 pytest；故命令若只寫 `python3` 會依 PATH 得到不同結果。
- `requirements.txt` 已含 `pytest>=7.0`，但沒有 `requirements.lock`、`environment.yml` 或 `conda-lock.yml`。
- 本輪只跑 smoke／report regression，未跑完整 Python test inventory，也沒有 current-HEAD full-test receipt。

## 6. State、文件與 evidence drift

### 6.1 P0：機械態互相矛盾

- `state/active.json`：mtime 2026-06-23、`cycles=[]`，所以 indexed active cycle = 0。
- `state/focus.json`：仍把已 superseded/concluded 的 G6 當 `now`，G6/G1 分別標成 `ACTIVE-PRIMARY` / `ACTIVE`。
- G1、G6 在 `active.json.recently_concluded` 已 concluded，但各自 `state.json` 仍是 `verdict=active`。
- `20260710-2244-layered-reconstruction-v2/state.json` 仍為 `active/P1_ORIENT`，卻不在 `active.json`，屬 reverse-orphan。
- 7/16 Hypercube、7/18 remediation、7/19 LongLineage rebuild、8/1 Exact-PS report 只有 audit 或局部 phase 狀態，未被一致 index/conclude。

### 6.2 P0/P1：live 文件落後

- `CURRENT_FOCUS` 頂端同時寫「in_progress」與 signed release 已 bounded stop；pinned 區仍停在 6/12。
- 2026-08-05 acceptance receipt 綁舊 HEAD `387a101e`，落後現行 HEAD；其中 pytest 缺、580 dirty、ahead 268 已過期。
- README 仍寫 265 tests / 38 suites；現存主 build 已是 270 / 39。`CURRENT_FOCUS` 更舊，仍寫 258 tests。
- `docs/handoff/README.md` 只索引 8/1，漏 8/5 與 8/6。
- `docs/experiments/INDEX.md` 最新到 7/27。
- `output/OUTPUT_INDEX.md` header 為 7/27、footer 仍是 4/14，且未列 8/1 observation report。
- `output/synthesis/master_run_manifest.tsv` 是 19 physical lines、18 data rows；18/18 都是 `tagged_bam_ready=false` / `partial`，需釐清「19 canonical runs」是文件計數口徑還是 manifest 漏列。

### 6.3 Ledger freshness

- `research/autoresearch/evidence_ledger.jsonl`：142/142 JSON valid，SHA-256 `009e1052…c272`。
- 只有 127 entries 有 timestamp；最大 timestamp 是 2026-07-27T23:30:00+08:00。
- 缺 all-sSNV Task-B 7/24 bounded stop、8/1 authority/report、8/5 engineering acceptance，故 coverage/freshness = PARTIAL。

## 7. Release / authority 的精確邊界

### 7.1 已通過

- 19/19 manifest paths 與 SHA-256。
- denominator registry 與 conservation checks。
- 8/1 observation report 的 release receipt `all_pass=true`。
- frozen technical result 可重複讀取，未發現 artifact drift。

### 7.2 尚未通過

- frozen binary 與 source 的 byte-reproducible relationship 未證明。
- environment lock 缺失。
- current clean HEAD build/test receipt 缺失。
- all-sSNV Task-B final result/report authority、formal reviews、receipts、signatures 缺失；nested finalizer 仍有 v29/v6 dependency split。
- browser QA receipt 有 3 個 path 指向已不存在的 `.staging`，雖 final artifact hashes 正確，provenance path 仍需修。
- branch 沒有 exact release tag。

結論：**可稱 data analysis / derived observation validated；不可稱 signed release completed。**

## 8. 五項研究啟動 gate

1. **Thread D？** 是，現有 read-level epigenetic overlay 服務 G3；但只能 association-only。舊 LOH-constrained Thread D 不是目前 live 主軸。
2. **Thread B 撤回範圍？** 維持 NO-GO；不可重新把 methylation 或 cross-sample whitelist 包裝成 FP filter。注意「撤回的 Thread B」與「all-sSNV Task-B 工作項」是兩個名稱相近但不同的概念。
3. **KDE-corrected？** Exact-PS / current lineage-tag branch不依賴 Coverage_Multiple KDE，應標 N/A；任何舊 TO/F1/CovM 結論若重用，必須另證明 KDE-corrected provenance。
4. **需要 caller AF？** 任何 F1、filter、CCF 或 biological AF 解讀都需要原始 caller VCF AF；現有 read-AF 不可替代。
5. **長計算 / C++ / 搬移 / NO-GO gate？** 有。全 7 rerun、10,717 abstain second pass、full likelihood M2、cleanup/搬移都需 pre-decision/resource gate；目前受磁碟與 release identity 阻塞。

額外 KB guardrails：LongPhase-S 的 HP=3 是 ambiguous somatic read-level tag，不可直接解讀為 LOH；HCC1395 benchmark 必須綁 HighConf/MedConf 與 HC BED；COLO829 沒有 callable BED 時只能報 truth-site sensitivity，不可報 WGS precision/F1。

## 9. 必做檢核與驗證清單

### P0 — 先恢復「可相信現在狀態」並維持長計算 block

1. 正規 reconcile `active.json`、cycle `state.json`、audit 與 `state/focus.json`。  
   → 驗證：G1/G6 不再同時 active + concluded；reverse-orphan 都被 index 或 conclude；`jq '.cycles|length' state/active.json` 等於實際 active state 數。

2. 補 LongLineage integration decision authority。  
   → 驗證：明示 A/B/C 路線、forest vs. single tree、tagged BAM artifact contract、claim ceiling，並綁 `3ac07d3c` / `b6f9b521`。

3. 維持 full-run block，先取得 `/big7_disk` 可用空間 ≥2T。  
   → 驗證：`df -h /big7_disk` 顯示 Avail ≥2T；任何清理都有核准清冊、Archive 目的地與可恢復記錄。

4. 處理稽核前既有的 16 項 working-tree 狀態；尤其 2 個 deleted snapshots 不可直接視為合法刪除。本輪新增的 audit report 應獨立 review/commit。  
   → 驗證：檔案被保留或依 archive policy 搬移；`git status --porcelain` 在 release commit 前為空；未覆蓋使用者既有變更。

5. 若目標是完成 all-sSNV Task-B signed release，必須由用戶明確重啟 release-repair cycle；否則維持 bounded stop。  
   → 驗證：final result/report authority、formal review、receipt、signature、transitive dependency test 全部存在且 hash-bound；不以 source authority 代替 result authority。

### P1 — 建立 current-HEAD 工程與證據 receipt

1. 在 clean/tagged commit 與新 build 目錄重做 configure/build/test。  
   → 驗證：receipt 綁 exact commit、branch、tag、source hash、binary hash、command、parameters；C++ `failed=0`，test count 以當次實際值為準。

2. 建 environment lock，固定 canonical Python。  
   → 驗證：lock hash 入 receipt；相同 lock 可重建 `/bip7_disk/.../python3` 等價環境；`pip check` exit 0。

3. 建 Python test allowlist 並跑完整適用矩陣。  
   → 驗證：receipt 列 collected、selected、skipped、passed、failed、duration；`failed=0`，任何排除都有理由。

4. 更新 ledger、OUTPUT_INDEX、CURRENT_FOCUS、README、handoff/experiments index。  
   → 驗證：ledger 含 7/24 bounded stop、8/1 authority/report、8/5 acceptance；OUTPUT_INDEX 能定位 8/1 artifact；README/CURRENT_FOCUS test counts 不再互相矛盾。

5. 修 authority portability。  
   → 驗證：browser QA receipt 直接指向 final artifacts；source–binary–environment relationship 可重建；8/5 immutable handoff 另指向 current acceptance v2，不覆寫歷史快照。

### P2 — 下一輪科學驗證（需先做 pre-decision audit）

1. 對 10,717 resource abstain 做分樣本 guard sensitivity，先處理 H2009 集中效應。  
   → 驗證：每樣本 completion/abstain、runtime/memory、guard 變動與 fail-closed receipt 完整；不只報 cohort average。

2. 若要推 directed topology，執行 preregistered full-scope strict topology；pilot 不可升級成全量結論。  
   → 驗證：7 technical datasets × chr1–22 都有 eligible receipt；abstain/tie/NOT_RUN 分開報；production topology completeness 不再是 0/7 才能更新 claim。

3. 若要推 biological lineage，補獨立生物證據。  
   → 驗證：CN/LOH/purity/multiplicity/CCF、AF stability/bootstrap，以及 single-cell、colony、multi-region/longitudinal 或同位點獨立複驗；未完成前不得稱 clone/ancestry。

4. benchmark 口徑固定。  
   → 驗證：HCC1395 綁 HC BED；COLO829 無 callable BED 時不報 WGS F1；caller AF 與 read-AF 欄位分離；HP=3 不被標成 LOH。

### P3 — 治理與文件衛生

1. 將 6 月 pinned focus 移入 historical/redirect；用 current baseline + lineage integration + explainer delivery 重建 live 首屏。
2. 修 readiness baseline：469 naming、439 metadata、1,381 links、35+2 legacy paths與 3 empty dirs只在 touched/new scope 漸進處理。
3. 修 `master_run_manifest.tsv` 18 data rows vs.「19 runs」口徑，避免 header 被當 run。

## 10. 本輪執行命令、輸入、輸出與實際片段

所有命令工作目錄：`/big7_disk/liaoyoyo2001/InterSubMod`。除本報告外，診斷結果只輸出到 stdout/stderr，沒有建立科學結果檔。

### 10.1 Authority hashes

輸入：`InterSubMod/docs/handoff/20260801_exactPS_readAF_CNV_AI交接_01/authority_manifest.json`

```bash
jq -r '(.artifacts[] | [.path, .sha256]),
       ([.implementation.frozen_binary.path, .implementation.frozen_binary.sha256]),
       (.implementation.source_snapshots[] | [.path, .sha256_at_handoff]) | @tsv' \
  docs/handoff/20260801_exactPS_readAF_CNV_AI交接_01/authority_manifest.json
# 對每一列執行 sha256sum -- "$artifact_path" 並比較 expected SHA
```

實際片段：`SUMMARY match=19 mismatch=0 missing=0`。  
輸出：stdout；既有 artifacts 未修改。

### 10.2 C++ tests

輸入：`InterSubMod/build/bin/run_tests`

```bash
./build/bin/run_tests
```

實際片段：`270 tests from 39 test suites`、`[ PASSED ] 270 tests`、exit 0。  
輸出：stdout/stderr；無新 release receipt。

### 10.3 Python smoke / report regression

```bash
PYTHONDONTWRITEBYTECODE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 \
  /bip7_disk/liaoyoyo2001/miniconda3/bin/python3 -m pytest \
  -q -p no:cacheprovider tests/test_drilldown_contract.py

PYTHONDONTWRITEBYTECODE=1 \
  /bip7_disk/liaoyoyo2001/miniconda3/bin/python3 -m unittest \
  research/20260801_exact_ps_observation_report/tests/test_build_exact_ps_observation_report.py \
  research/20260801_exact_ps_observation_report/tests/test_finalize_exact_ps_observation_report.py \
  research/20260801_exact_ps_observation_report/tests/test_qa_exact_ps_observation_report.py
```

實際片段：`15 passed in 3.08s`；`Ran 12 tests`、`OK`；兩者 exit 0。  
輸出：stdout/stderr。

### 10.4 Readiness

輸入：InterSubMod repo 與 canonical output symlink。

```bash
bash scripts/analysis/check_ai_agent_readiness.sh
```

實際片段：`PASS: 14`、`WARN: 6`、`FAIL: 0`；exit 0。  
輸出：stdout。

### 10.5 Git / disk

```bash
git status --short
git rev-list --left-right --count '@{upstream}...HEAD'
git ls-remote origin refs/heads/feat/lineage-tag-methylation-axes
df -h /big7_disk /bip7_disk
```

實際片段：診斷當下 16 status entries；本報告產生後另多 1 個 untracked report；upstream `0 0`；remote/local HEAD `4de8b1d0…`；`/big7_disk` 617G/99%、`/bip7_disk` 1.2T/96%。  
輸出：stdout。

### 10.6 State / ledger

```bash
jq '{updated_at, cycles, recently_concluded}' state/active.json
jq '.' state/focus.json
jq -e . research/autoresearch/evidence_ledger.jsonl >/dev/null
jq -s '{total_entries:length, latest_timestamp:(map(.timestamp // "") | max)}' \
  research/autoresearch/evidence_ledger.jsonl
```

實際片段：`cycles=[]`；focus 仍 pin G6/G1；ledger 142 entries、JSON valid、latest timestamp `2026-07-27T23:30:00+08:00`。  
輸出：stdout。

## 11. 子任務稽核紀錄

| 子任務 | 輸入 | 預期輸出／驗證 | 實際結果 |
|---|---|---|---|
| state/docs audit | `CURRENT_FOCUS`、state registry、handoff/index、git log | 找出真正 live focus 與衝突；每項附 path/value | 找到三層主軸、reverse-orphan cycles、G1/G6 active/concluded 矛盾與導航 drift |
| engineering audit | Git、disk、build runners、Python env、readiness | exit code、test count、binary identity、gate pass/fail | 2/4 舊工程 gates 通過；270 C++、15 pytest、12 unittest 通過；disk/dirty/clean-build/lock 未過 |
| evidence audit | ledger、manifest、denominator、Task-B receipts、8/1 report | hash/existence/denominator/claim ceiling 全量核對 | 19/19 MATCH；ledger coverage partial；technical result可信但 signed release/biological lineage 不成立 |

## 12. 下一個需要用戶裁決的分岔

完成 P0 state/disk/repo identity 後，需由用戶選擇下一個高投入方向：

1. **Release repair**：完成 all-sSNV signed result/report authority；
2. **Scientific completion**：處理 10,717 abstain、full strict topology 與生物驗證；
3. **Delivery first**：先完成 lineage-tag contract、解釋中心與 current acceptance v2。

三者都合理，但會各自消耗顯著時間；在 P0 前不應默默啟動其中任何一條。
