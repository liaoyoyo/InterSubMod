# Reproducible local browser/HTML QA environment (Python >=3.10).
# Chromium itself is installed separately with:
#   python -m playwright install chromium
lxml==6.0.2
Pillow==12.3.0
playwright==1.60.0
pytest==8.4.2
