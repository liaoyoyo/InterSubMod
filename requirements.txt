# InterSubMod Python Dependencies
# Install with: pip install -r requirements.txt
#
# Python >=3.10 is required.  Current source and tests use PEP 604 unions and
# dataclass(slots=True), which Python 3.9 cannot import.
# Scientific dependency lower bounds originate from the 2026-08-05 bip7
# environment audit; portable CI resolves them through requirements-ci.lock.
# 本檔為「直接依賴」清單；完整傳遞依賴的精確鎖定請另跑 `pip freeze`。

# --- Core scientific computing ---
numpy>=1.23.5
pandas>=1.5.1
scipy>=1.13.1

# --- Bioinformatics I/O ---
# pysam 是讀 BAM/VCF 的核心依賴，缺它多數分析腳本無法執行
pysam>=0.23.3

# --- Machine learning / statistics ---
scikit-learn>=1.6.1
statsmodels>=0.14.5

# --- Visualization ---
matplotlib>=3.6.2
seaborn>=0.13.2
Pillow>=9.3.0

# --- Report / slide generation ---
python-pptx>=1.0.2
lxml>=6.0.2
PyYAML>=6.0.3

# --- Testing ---
# pytest 是 portable CI 的必要直接依賴；測試數與 suite 數必須在當次
# clean checkout 動態產生，不在 dependency manifest 手抄。
pytest>=7.0
jsonschema>=4.23,<5

# --- Optional ---
# matplotlib-cjk-fonts  # CJK 字型支援（本專案已改用 scripts/lib/plot_setup.py 處理）
